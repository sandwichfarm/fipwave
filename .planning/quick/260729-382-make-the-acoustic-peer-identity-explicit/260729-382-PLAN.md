---
quick_id: 260729-382
status: in_progress
description: Make the acoustic peer identity explicit across runner configuration so documented arbitrary machine IDs can complete the FAS1 handshake.
must_haves:
  truths:
    - The browser receives an expected acoustic peer identity chosen by its runner.
    - The FAS1 HELLO handshake validates that configured peer identity instead of a hard-coded name.
    - The documented two-laptop commands provide reciprocal machine and peer identities.
  artifacts:
    - packages/bridge/src/runner.ts
    - packages/bridge/src/server.ts
    - apps/modem-ui/src/quiet-client.ts
    - apps/modem-ui/src/main.ts
    - docs/laptop-to-laptop-test.md
  key_links:
    - Runner CLI/options to qualification-config to fetchRunnerConfig to AcousticSession.expectedPeer
---

# Explicit Acoustic Peer Identity

1. Add an optional runner/CLI `peerMachineId` setting, preserving the current
   role-derived names only as a compatibility default. Project it safely through
   the runner configuration endpoint.
2. Validate the projected field in the browser and pass it into the FAS1
   session's expected-peer contract.
3. Update the physical-run instructions and regression tests for reciprocal,
   non-default machine IDs; run focused and full relevant validation.
