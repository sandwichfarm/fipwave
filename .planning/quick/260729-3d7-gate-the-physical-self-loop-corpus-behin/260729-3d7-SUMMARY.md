---
quick_id: 260729-3d7
status: complete
date: 2026-07-29
branch: feat/acoustic-fast-reliable-link
commits:
  - 70958ea
---

# FAS1-Ready Physical Harness Gate Summary

The physical self-loop harness now waits for both FAS1 sessions to report
committed settings and a current heartbeat before starting its legacy Quiet
corpus. This prevents the corpus from racing the actual connection process.

## Delivered

- Added a bounded `--session-ready-timeout-ms` option (default 60 seconds).
- Added readiness-progress and ready events with the complete bounded acoustic
  card copy, so a timeout identifies the failed handshake phase on each role.
- Gated corpus A→B and B→A playback behind both pages reporting
  `Ready · evidence: Loopback · FIPS ready`.
- Added timeout parsing coverage.

## Physical result

On the local MacBook Pro built-in speaker → air → built-in microphone path,
the gated run reached ready state after about 35 seconds and then exchanged
one 256-byte message each direction byte-perfectly. It used no virtual audio
or fake media and restored the original output/input levels after cleanup.

Evidence: `.artifacts/diagnostics/self-loop/20260729T012646992Z-29282/summary.json`.

## Verification

- `npm run check` passed: dependency audit, lint, strict TypeScript, 316 unit
  tests, 22 Chromium tests, production build, corpus/qualification, and Compose
  checks.
