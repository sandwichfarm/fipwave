---
quick_id: 260729-3d7
status: in_progress
description: Gate the physical self-loop corpus behind real FAS1 acoustic readiness and capture readiness failures for handshake diagnosis.
must_haves:
  truths:
    - The self-loop harness does not send the legacy corpus before both FAS1 sessions are ready.
    - A bounded readiness timeout records the observed acoustic state for diagnosis.
    - The new timeout option is validated by unit tests.
  artifacts:
    - scripts/self-loop-smoke.mjs
    - tests/self-loop-smoke.test.ts
  key_links:
    - Quiet arm to FAS1 session readiness to corpus transmission
---

# FAS1-Ready Physical Harness Gate

1. Add a bounded session-readiness timeout and DOM-based FAS1-ready wait after
   Quiet arms but before corpus playback.
2. Persist readiness progress and the final observed session copies in the
   physical diagnostic event log.
3. Add option parsing coverage and run the focused/full checks, then use the
   guarded harness to obtain real hardware evidence.
