---
quick_id: 260729-2ly
status: complete
mode: quick-full
description: Improve acoustic connection speed and reliability with erasure correction, fast same-epoch resume, robust ceremony transmission, and live throughput.
must_haves:
  truths:
    - One missing CRC-valid-or-rejected DATA fragment per parity group is reconstructed without a retransmission turn.
    - Automatic same-epoch reconnection reuses mutually proven settings and skips probes, while mismatched or failed warm settings fall back to calibration.
    - Ordinary maximum-MTU delivery uses a larger bounded burst without weakening explicit half-duplex ownership.
    - Ceremony units use immutable redundant per-job transmission settings while probes and data use the selected candidate.
    - Live TX and RX payload rate updates in the UI and selects bps, kbps, or Mbps by magnitude.
  artifacts:
    - apps/modem-ui/src/acoustic-protocol.ts
    - apps/modem-ui/src/acoustic-session.ts
    - apps/modem-ui/src/quiet-client.ts
    - apps/modem-ui/src/throughput.ts
    - apps/modem-ui/src/main.ts
  key_links:
    - FAS1 parity geometry is CRC-protected and feeds normal reassembly only after exact one-erasure recovery.
    - Resume CAPS binds new nonces and the prior settings digest before normal commit and heartbeat readiness gates.
    - Session delivered-byte counters feed a rolling browser sampler rather than deriving speed from queued bytes.
---

# Quick Task 260729-2ly Plan

## Task 1: Add bounded erasure correction and faster packet bursts

**Files:** `apps/modem-ui/src/acoustic-protocol.ts`,
`apps/modem-ui/src/acoustic-protocol.test.ts`,
`apps/modem-ui/src/acoustic-session.ts`,
`apps/modem-ui/src/acoustic-session.test.ts`

**Action:** Add a CRC-bound parity FAS1 unit, pure XOR parity/recovery helpers,
four-data-fragment parity groups, and an eight-data-fragment ordinary burst.
Integrate parity into receive-side assembly before bitmap ACK generation.
Expose parity/recovery and completed-payload byte counters.

**Verify:** Protocol tests cover parity geometry, one erasure, no erasure,
multiple erasures, a short final fragment, and malformed units. Session tests
prove one dropped data frame is delivered without retransmitting it, two losses
remain missing, and maximum-MTU delivery uses the wider bounded burst.

**Done:** A single erased DATA fragment per group is recovered and normal
delivery/ownership invariants still pass.

## Task 2: Add robust ceremony jobs and mutually authenticated fast resume

**Files:** `apps/modem-ui/src/acoustic-session.ts`,
`apps/modem-ui/src/acoustic-session.test.ts`,
`apps/modem-ui/src/quiet-client.ts`,
`apps/modem-ui/src/quiet-client.test.ts`,
`apps/modem-ui/src/main.ts`

**Action:** Add per-unit modem transmission modes, capture the selected
repetition/guard/gain in each queued Quiet job, and use a redundant ceremony
setting only for HELLO/HELLO_ACK/CAPS/COMMIT/COMMIT_ACK/RESET. Preserve
same-epoch proven settings, exchange their digest in nonce-bound CAPS, skip
probes only on exact mutual match, and invalidate/fall back after a bounded
failed resume bootstrap.

**Verify:** Tests prove queued jobs cannot observe later candidate mutation,
ceremony and data settings differ as intended, same-epoch reconnect reaches
Ready with no new probe measurements, new-epoch/digest mismatch calibrates,
and a failed resume invalidates the warm record.

**Done:** Link failure still performs a fresh handshake, but calibration is
skipped until the proven settings fail the bounded bootstrap.

## Task 3: Add real-time human-readable throughput

**Files:** `apps/modem-ui/src/throughput.ts`,
`apps/modem-ui/src/throughput.test.ts`,
`apps/modem-ui/src/acoustic-status.ts`,
`apps/modem-ui/src/acoustic-status.test.ts`,
`apps/modem-ui/src/main.ts`

**Action:** Track acknowledged TX and delivered RX payload bytes, sample them
on a bounded rolling window twice per second, format rates as bps/kbps/Mbps,
and render live TX/RX rates in both demo and diagnostic link statistics.

**Verify:** Unit tests cover rolling-rate decay/reset and all unit thresholds;
typecheck, lint, modem-ui unit suite, and production build pass.

**Done:** Stats visibly update without packet events and report current payload
rate with the appropriate unit.
