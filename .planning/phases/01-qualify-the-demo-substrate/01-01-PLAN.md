---
phase: 01-qualify-the-demo-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .node-version
  - scripts/audit-dependencies.mjs
  - tests/dependency-audit.test.mjs
  - .planning/phases/01-qualify-the-demo-substrate/dependency-audit.json
autonomous: true
requirements:
  - CODEC-02
  - CODEC-03
  - CODEC-04
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-07
  - DEPLOY-02

must_haves:
  truths:
    - "The qualification workspace is pinned to Node 22.23.1 before any package installation."
    - "Every direct npm package has machine-readable identity, engine, repository, release-time, integrity, and audit-result evidence from the authoritative registry."
    - "The audit contains exactly the eight approved package/version pairs and can later reject a lockfile that diverges from them."
  artifacts:
    - path: ".node-version"
      provides: "Exact Node 22.23.1 runtime pin"
      contains: "22.23.1"
    - path: "scripts/audit-dependencies.mjs"
      provides: "Registry evidence collector and lockfile conformance verifier"
    - path: ".planning/phases/01-qualify-the-demo-substrate/dependency-audit.json"
      provides: "Canonical machine-readable dependency evidence"
  key_links:
    - from: "scripts/audit-dependencies.mjs"
      to: ".planning/phases/01-qualify-the-demo-substrate/dependency-audit.json"
      via: "authoritative npm packument metadata normalized into the pinned audit schema"
      pattern: "engines|repository|integrity|publishedAt|auditResult"
    - from: ".planning/phases/01-qualify-the-demo-substrate/dependency-audit.json"
      to: "package-lock.json"
      via: "Plan 01-02 invokes the same script in lockfile-check mode before npm ci"
      pattern: "check-lock|package-lock"
  assumptions:
    - requirement: CODEC-02
      category: supply-chain
      status: resolved
      assumption: "Authoritative registry metadata and immutable integrity values are sufficient deterministic evidence for the exact packages locked by the checker."
      verification: automated
  prohibitions:
    - status: flagged-unverified
      text: "Must not claim fixture or loopback evidence as physical two-laptop qualification."
    - status: flagged-unverified
      text: "Must not expose the local browser bridge beyond loopback."
    - status: flagged-unverified
      text: "Must not grant privileged mode or SYS_ADMIN to the TUN preflight."
---

## Phase Goal

**As a** demo operator, **I want to** qualify the exact two laptops' audible
browser-audio codec path and Docker/TUN substrate, **so that** FIPS integration
begins only with evidence-backed codec and host assumptions.

<objective>
Create autonomous, reproducible supply-chain evidence for the exact Phase 1
runtime and direct npm dependencies before deterministic scaffolding installs
them.

Purpose: The walking skeleton must consume a fixed, auditable dependency set
without inventing a human approval gate before implementation.

Output: Node runtime pin, registry-audit utility, canonical dependency audit,
and a deterministic audit test.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md
@.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md
@.planning/phases/01-qualify-the-demo-substrate/01-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pin Node and emit authoritative dependency evidence</name>
  <reversibility rating="costly">The runtime and direct dependency majors shape every Phase 1 build command, but remain replaceable through one coordinated lockfile and configuration update.</reversibility>
  <files>.node-version, scripts/audit-dependencies.mjs, tests/dependency-audit.test.mjs, .planning/phases/01-qualify-the-demo-substrate/dependency-audit.json</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md` — researched Node/TypeScript/Vite/Vitest/Playwright/ws toolchain and validation commands.
    - `.planning/phases/01-qualify-the-demo-substrate/01-VALIDATION.md` — Wave 0 consumers that require deterministic package evidence.
  </read_first>
  <behavior>
    - The runtime pin is exactly `22.23.1`; audit generation refuses a different active Node version.
    - The audit contains exactly `vite@8.1.5`, `typescript@7.0.2`, `vitest@4.1.10`, `@playwright/test@1.61.1`, `ws@8.21.1`, `eslint@10.7.0`, `@types/node@26.1.1`, and `@types/ws@8.18.1`.
    - Each package record includes name, exact version, declared Node engines, normalized repository URL, `dist.integrity`, version publication timestamp, registry fetch timestamp, and `auditResult: "pass"`.
    - Audit generation fails on missing metadata, a name/version mismatch, a repository outside the expected upstream allowlist, absent integrity, or an engine range incompatible with Node 22.23.1.
    - Lockfile-check mode compares direct dependency versions and package integrities to this audit and fails on any missing, added, or divergent direct package.
  </behavior>
  <action>
Write `.node-version` with exact runtime `22.23.1`. Implement the audit utility
using only Node core APIs and authoritative npm registry packuments. Hard-code
the eight checker-approved package/version pairs and expected upstream
repository identities, normalize common Git URL forms, evaluate each declared
Node engine against `22.23.1`, and record both the version publication time and
the current registry-fetch time. Persist the JSON through a temporary file and
atomic rename with a schema version and overall pass result.

Add `--check` mode for schema, exact-set, metadata, and allowlist validation, and
`--check-lock &lt;path&gt;` mode for later direct lockfile version/integrity
comparison. Start with tests that reject a missing package, extra direct
package, wrong version, incompatible engine, wrong repository, absent
integrity, and divergent lockfile. Generate the canonical audit only after all
records pass. Do not install packages or request human approval in this plan.
  </action>
  <verify>
    <automated>test "$(cat .node-version)" = "22.23.1" &amp;&amp; node scripts/audit-dependencies.mjs --check .planning/phases/01-qualify-the-demo-substrate/dependency-audit.json &amp;&amp; node --test tests/dependency-audit.test.mjs</automated>
  </verify>
  <acceptance_criteria>
    - The audit contains the exact eight package/version pairs and no others.
    - Every package record contains version, Node engines, repository URL, integrity, registry/release timestamps, and a passing audit result.
    - The test proves lockfile-check mode rejects both version and integrity drift.
    - No package installation or human checkpoint occurs.
  </acceptance_criteria>
  <done>Plan 01-02 can install only the exact dependency set proven by the canonical autonomous audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → audit artifact | Third-party identity and archive metadata become trusted inputs to later installation. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | direct npm package selection | high | mitigate | Exact allowlisted names/versions, upstream repository checks, integrity capture, Node-engine evaluation, and lockfile conformance tests before installation. |
| T-01-01 | Spoofing | similarly named package | high | mitigate | Reject any name or normalized repository URL outside the fixed checker-approved set. |
| T-01-02 | Repudiation | dependency evidence | medium | mitigate | Persist schema-versioned registry timestamps, release timestamps, integrity values, per-record results, and one overall result. |
</threat_model>

## Artifacts this phase produces

| File | Symbols / contract |
|------|--------------------|
| `.node-version` | exact Node `22.23.1` runtime |
| `scripts/audit-dependencies.mjs` | registry evidence generation, `--check`, and `--check-lock` |
| `.planning/phases/01-qualify-the-demo-substrate/dependency-audit.json` | exact package metadata and audit results |

<verification>
<automated>node scripts/audit-dependencies.mjs --check .planning/phases/01-qualify-the-demo-substrate/dependency-audit.json &amp;&amp; node --test tests/dependency-audit.test.mjs</automated>
</verification>

<success_criteria>
- The runtime and every direct dependency are exact and machine-auditable.
- The audit captures all checker-required evidence fields.
- Later lockfile drift is detectable before installation.
</success_criteria>

<output>
Create `.planning/phases/01-qualify-the-demo-substrate/01-01-SUMMARY.md` when done.
</output>
