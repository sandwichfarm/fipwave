---
phase: 01-qualify-the-demo-substrate
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - apps/modem-ui/src/main.ts
  - apps/modem-ui/src/audio.ts
  - apps/modem-ui/public/worklets/pcm-capture.js
  - apps/modem-ui/src/audio.test.ts
  - apps/modem-ui/src/style.css
  - apps/modem-ui/e2e/audio-preflight.spec.ts
autonomous: true
requirements:
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-07

must_haves:
  truths:
    - "D-01: One explicit Arm modem action requests microphone permission, starts or resumes audio, and arms capture/playback."
    - "D-02 and D-03: Readiness uses the actually applied mono, sample-rate, processing, AudioContext, and AudioWorklet evidence; unknown or incompatible values fail visibly."
    - "D-04: One production browser build and one state machine are portable to both exact Chromium laptops."
    - "Reset invalidates stale asynchronous results, closes owned audio resources, and begins one new epoch without erasing historical evidence."
    - "The 320px overflow and unusually long device/error text UI cases remain structured visual backstops, never silently assumed."
  artifacts:
    - path: "apps/modem-ui/src/audio.ts"
      provides: "Browser audio arm/reset, applied-settings evaluation, and bounded PCM playback"
      exports: ["armAudio", "evaluateAppliedSettings", "validatePcmPlaybackFrame", "createPlaybackQueue", "enqueuePcmPlayback", "resetAudio"]
    - path: "apps/modem-ui/public/worklets/pcm-capture.js"
      provides: "Fixed-size timestamped Float32 capture batches"
    - path: "apps/modem-ui/e2e/audio-preflight.spec.ts"
      provides: "Deterministic browser state and accessibility tests"
  key_links:
    - from: "apps/modem-ui/src/main.ts"
      to: "apps/modem-ui/src/audio.ts"
      via: "Arm/reset handlers call the epoch-scoped audio lifecycle"
      pattern: "armAudio|evaluateAppliedSettings|resetAudio"
    - from: "apps/modem-ui/src/audio.ts"
      to: "apps/modem-ui/public/worklets/pcm-capture.js"
      via: "AudioWorklet registration after the user gesture"
      pattern: "audioWorklet\\.addModule|AudioWorkletNode"
    - from: "apps/modem-ui/src/main.ts"
      to: "apps/modem-ui/src/audio.ts"
      via: "PCM_PLAYBACK frames enter only the validated bounded AudioContext scheduler"
      pattern: "validatePcmPlaybackFrame|enqueuePcmPlayback"
  ui_considerations:
    - category: empty
      status: covered
      verification: automated
    - category: loading
      status: covered
      verification: automated
    - category: error
      status: covered
      verification: automated
    - category: populated
      status: covered
      verification: automated
    - category: partial
      status: covered
      verification: automated
    - category: zero-one-many
      status: covered
      verification: automated
    - category: overflow
      status: backstop
      verification: backstop
    - category: long-text
      status: backstop
      verification: backstop
  assumptions:
    - requirement: WEB-01
      category: concurrency
      status: resolved
      assumption: "Exactly one arm operation may be active per epoch; repeated activation while requesting is disabled and reset invalidates late completion."
      verification: automated
    - requirement: WEB-02
      category: concurrency
      status: resolved
      assumption: "Applied settings are committed as one epoch-scoped snapshot; stale asynchronous snapshots cannot change readiness."
      verification: automated
    - requirement: WEB-03
      category: unclassified
      status: flagged-unverified
      assumption: "The explicit applied-settings failure matrix is authoritative pending exact-device evidence."
      verification: flagged
    - requirement: WEB-07
      category: unclassified
      status: flagged-unverified
      assumption: "Exact-laptop portability remains physical evidence rather than an inferred automated result."
      verification: flagged
  prohibitions:
    - status: flagged-unverified
      text: "Must not claim fixture or loopback evidence as physical two-laptop qualification."
    - status: flagged-unverified
      text: "Must not expose the local browser bridge beyond loopback."
    - status: flagged-unverified
      text: "Must not grant privileged mode or SYS_ADMIN to the TUN preflight."
---

## Phase Goal

**As a** demo operator, **I want to** qualify the exact two laptops' audible
browser-audio codec path and Docker/TUN substrate, **so that** FIPS integration
begins only with evidence-backed codec and host assumptions.

<objective>
Expand the walking skeleton into the complete one-action browser-audio
preflight and its honest operator states.

Purpose: The physical qualification gate may run only when actual browser
settings—not requested constraints or mocked success—prove the selected audio
format is available.

Output: Browser audio lifecycle, capture worklet, applied-settings evaluator,
accessible preflight UI, and deterministic unit/browser-state tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md
@.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md
@.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md
@.planning/phases/01-qualify-the-demo-substrate/01-VALIDATION.md
@.planning/phases/01-qualify-the-demo-substrate/01-02-SUMMARY.md
</context>

<interfaces>
From Plan 01-02:

- The production shell already exposes semantic `Arm modem` and a loopback-only
  binary/report seam.
- `createBridgeServer(...)` owns local connection state and report persistence.

This plan creates:

- `armAudio(epoch: number): Promise&lt;AppliedAudioEvidence&gt;`
- `evaluateAppliedSettings(evidence: AppliedAudioEvidence): AudioPreflightResult`
- `validatePcmPlaybackFrame(data: ArrayBuffer, currentEpoch: number): PcmPlaybackChunk`
- `createPlaybackQueue(options: { maxBytes: number; maxDurationMs: number }): PcmPlaybackQueue`
- `enqueuePcmPlayback(chunk: PcmPlaybackChunk): PlaybackMetrics`
- `resetAudio(): Promise&lt;number&gt;`
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Carry one arm gesture through actual settings and PCM capture</name>
  <files>apps/modem-ui/src/audio.ts, apps/modem-ui/public/worklets/pcm-capture.js, apps/modem-ui/src/audio.test.ts, apps/modem-ui/src/main.ts</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md` — D-01 through D-04 one-action and applied-settings decisions.
    - `.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md` — exact media constraints, state failures, AudioWorklet, and reset/epoch behavior.
    - `.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md` — enabled-control and applied-evidence contracts.
    - `packages/bridge/src/server.ts` — binary settings/report path created by the walking skeleton.
  </read_first>
  <behavior>
    - One click creates or resumes a 48 kHz AudioContext, requests ideal mono/48 kHz capture and exact disabling of echo cancellation, noise suppression, and automatic gain control, then registers the worklet.
    - Readiness is calculated from `MediaStreamTrack.getSettings()`, actual context state/rate, permission, and worklet state.
    - Unknown permission, unavailable microphone, non-running context, non-mono channel count, incompatible rates, enabled processing, unavailable worklet, or bridge loss produces an exact failure and cannot reveal qualification controls.
    - Timestamped fixed-size Float32 chunks include sample index, sample rate, channel count, encoding, epoch, and discontinuity metadata.
    - Browser-side `PCM_PLAYBACK` validation rejects bad magic/version/type/length/epoch/rate/channels/encoding/alignment before queueing.
    - A byte/time-bounded playback queue schedules accepted samples monotonically on the armed AudioContext destination and reports discontinuity plus high-water metrics.
    - Overflow fails without partial scheduling; reset cancels queued/scheduled sources and stale-epoch frames or callbacks.
    - Reset stops owned tracks, closes/suspends audio, clears browser queues, increments epoch, and rejects late completion from the prior epoch.
  </behavior>
  <action>
Start with failing tests for the successful one-gesture path and every
incompatible applied setting. Implement `armAudio`, `evaluateAppliedSettings`,
and `resetAudio` with the exact D-01 through D-03 constraints. Keep one
in-flight arm promise per epoch; repeated activation while requesting does
nothing, and reset invalidates late media/worklet completion.

Implement the AudioWorklet processor for fixed-size timestamped Float32 batches
only. It must report discontinuities and never perform codec work. Connect the
existing skeleton control to the lifecycle and binary `AUDIO_SETTINGS` path,
using actual values in the report. Keep fixture and mocked evidence explicitly
non-physical.

In `audio.ts`, implement `validatePcmPlaybackFrame`, `createPlaybackQueue`, and
`enqueuePcmPlayback`. Validate the full research-defined `PCM_PLAYBACK` header
and Float32 payload before allocation or queueing. Bound playback by configured
bytes and scheduled milliseconds; preserve first-sample indices; detect gaps
and overlaps; report byte/time high-water and discontinuities. Convert accepted
mono Float32 samples into AudioBufferSourceNodes and schedule them on the same
armed AudioContext destination at a monotonic next-playback time. Track all
queued/scheduled sources by epoch so `resetAudio` stops them, clears the queue,
and invalidates stale frames and callbacks. Add fake-AudioContext tests for
malformed frames, both caps, monotonic scheduling, metrics, overflow, reset
cancellation, and stale epochs.
  </action>
  <verify>
    <automated>npm run test:unit -- apps/modem-ui/src/audio.test.ts &amp;&amp; npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Requested constraints and applied evidence match WEB-01, WEB-02, WEB-03, and D-01 through D-03.
    - The complete failure matrix blocks readiness and includes the exact observed value.
    - Concurrency, reset-during-request, and stale-completion tests pass.
    - PCM batches are timestamped, format-declared, epoch-scoped, and discontinuity-aware.
    - Playback tests reject malformed/stale frames before queueing, enforce byte and duration caps, and never partially schedule overflow.
    - Accepted samples schedule monotonically on the armed destination and expose discontinuity plus byte/time high-water evidence.
    - Reset stops queued/scheduled sources; prior epochs cannot schedule samples or mutate current playback metrics.
  </acceptance_criteria>
  <done>The real browser lifecycle can arm one local epoch and produce honest audio/settings evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the complete accessible audio-preflight state machine</name>
  <files>apps/modem-ui/src/main.ts, apps/modem-ui/src/style.css, apps/modem-ui/e2e/audio-preflight.spec.ts</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md` — exact copy, colors, type, spacing, state transitions, accessibility, and responsive behavior.
    - `apps/modem-ui/src/audio.ts` — applied evidence and epoch lifecycle created in Task 1.
    - `apps/modem-ui/index.html` — semantic shell produced by Plan 01-02.
  </read_first>
  <behavior>
    - `idle`, `requesting`, `ready`, `failed`, and `disconnected` expose exactly the specified copy, enabled controls, live-region announcements, and applied-value rows.
    - Dynamic device labels and errors enter the DOM as text, never HTML.
    - Ready reveals `Start Cyrinx qualification`; failure/disconnect expose only `Reset / re-arm`; reset preserves historical rows.
    - The 320px layout keeps controls usable and long values wrapping; these two cases remain visual backstops with automated structural coverage.
  </behavior>
  <action>
Render the UI-SPEC operator/control and applied-audio evidence cards using
semantic controls, key/value markup, text status, visible focus, required
system fonts, fixed color/spacing tokens, polite announcements, and assertive
hard-failure announcements. Do not add device selection, waveform, spectrum,
audio meter, autoplay, or codec tuning controls.

Use Playwright media mocks only for permission denial, incompatible applied
settings, bridge disconnect, reset, stale epoch, and success-state rendering.
Assert exact copy and control availability. Add deterministic narrow-viewport
and long-value structural checks, while retaining the two UI-SPEC entries as
human visual backstops. Build one artifact for both D-04 laptops; do not infer
hardware portability from Playwright.
  </action>
  <verify>
    <automated>npm run test:browser -- apps/modem-ui/e2e/audio-preflight.spec.ts &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - All five states and recovery transitions match the UI-SPEC.
    - Unknown or incompatible evidence never exposes the qualification-start action.
    - Status is never conveyed by color alone; focus, live regions, labels, and minimum target sizes are enforced.
    - Overflow and long-text tests pass while remaining declared visual backstops for the final browser checkpoint.
  </acceptance_criteria>
  <done>The same production console gives an accessible, fact-based audio preflight on either selected Chromium laptop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/device → readiness | Permission results, applied settings, device labels, and asynchronous state are untrusted observations. |
| AudioWorklet → main thread | PCM metadata and discontinuity signals cross an asynchronous epoch boundary. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | audio readiness | high | mitigate | Derive readiness from one epoch-scoped applied-settings snapshot and test every unknown/incompatible input. |
| T-01-06 | Denial of service | arm/reset concurrency | medium | mitigate | One in-flight arm per epoch, bounded PCM batches, owned resource cleanup, and stale completion rejection. |
| T-01-07 | Information disclosure / XSS | device labels and errors | medium | mitigate | Persist only required post-permission fields and render all dynamic observations through text-only DOM APIs. |
</threat_model>

## Artifacts this phase produces

| File | Symbols / contract |
|------|--------------------|
| `apps/modem-ui/src/audio.ts` | `armAudio`, `evaluateAppliedSettings`, `validatePcmPlaybackFrame`, `createPlaybackQueue`, `enqueuePcmPlayback`, `resetAudio` |
| `apps/modem-ui/public/worklets/pcm-capture.js` | fixed-size timestamped Float32 PCM batches |
| `apps/modem-ui/src/main.ts` | `idle/requesting/ready/failed/disconnected` controller |

<verification>
<automated>npm run test:unit -- apps/modem-ui/src/audio.test.ts &amp;&amp; npm run test:browser -- apps/modem-ui/e2e/audio-preflight.spec.ts &amp;&amp; npm run build</automated>
</verification>

<success_criteria>
- One action owns the complete permission/audio/worklet arm flow.
- Actual settings determine readiness and exact failure copy.
- PCM playback is validated, byte/time bounded, scheduled on the armed AudioContext, and metric-producing.
- Reset and stale asynchronous state are epoch-safe.
- The production artifact preserves all structured UI considerations and backstops.
</success_criteria>

<output>
Create `.planning/phases/01-qualify-the-demo-substrate/01-03-SUMMARY.md` when done.
</output>
