---
phase: 01-qualify-the-demo-substrate
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vite.config.ts
  - vitest.config.ts
  - playwright.config.ts
  - eslint.config.js
  - apps/modem-ui/index.html
  - packages/bridge/src/server.ts
  - tests/skeleton.test.ts
autonomous: true
requirements:
  - CODEC-02
  - CODEC-03
  - WEB-01
  - WEB-03

must_haves:
  truths:
    - "The lockfile uses Node 22.23.1 and exactly matches the direct versions and integrities in the autonomous dependency audit."
    - "One production browser action crosses a binary loopback-only WebSocket path and produces a canonical, explicitly non-physical JSON report."
    - "Wave 0 exposes terminating lint, typecheck, unit, browser, build, fixture, corpus, Compose, and selection-verifier commands without requiring hardware."
  artifacts:
    - path: "package.json"
      provides: "Exact runtime/dependency pins and complete Phase 1 command surface"
    - path: "packages/bridge/src/server.ts"
      provides: "Loopback-only browser route, binary WebSocket tracer, and report write/read seam"
      exports: ["createBridgeServer", "writeQualificationReport", "readQualificationReport"]
    - path: "tests/skeleton.test.ts"
      provides: "End-to-end binary loopback and report-persistence tracer"
  key_links:
    - from: ".planning/phases/01-qualify-the-demo-substrate/dependency-audit.json"
      to: "package-lock.json"
      via: "audit utility lockfile-check mode runs before npm ci"
      pattern: "verify:dependencies|check-lock"
    - from: "apps/modem-ui/index.html"
      to: "packages/bridge/src/server.ts"
      via: "same-origin binary WebSocket on a loopback-bound server"
      pattern: "WebSocket|127\\.0\\.0\\.1|localhost"
    - from: "packages/bridge/src/server.ts"
      to: ".artifacts/qualification/"
      via: "atomic schema-versioned JSON write followed by read"
      pattern: "writeQualificationReport|readQualificationReport"
  assumptions:
    - requirement: WEB-01
      category: persistence
      status: resolved
      assumption: "Canonical per-machine JSON is the walking-skeleton persistence seam; this operator tool has no shared mutable database requirement."
      verification: automated
  prohibitions:
    - status: flagged-unverified
      text: "Must not claim fixture or loopback evidence as physical two-laptop qualification."
    - status: flagged-unverified
      text: "Must not expose the local browser bridge beyond loopback."
    - status: flagged-unverified
      text: "Must not grant privileged mode or SYS_ADMIN to the TUN preflight."
---

## Phase Goal

**As a** demo operator, **I want to** qualify the exact two laptops' audible
browser-audio codec path and Docker/TUN substrate, **so that** FIPS integration
begins only with evidence-backed codec and host assumptions.

<objective>
Build the Phase 1 walking skeleton and complete Wave 0: an operator action
traverses the real browser route, binary localhost bridge, and canonical JSON
persistence using only dependency-audited, exactly locked packages.

Purpose: Prove the local architecture and every deterministic command before
browser-audio, qualification, and TUN expansions depend on it.

Output: Exact package lock, TypeScript/Vite/Vitest/Playwright/ESLint
configuration, production route, loopback tracer server, report persistence,
and runnable validation scripts.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-qualify-the-demo-substrate/01-SKELETON.md
@.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md
@.planning/phases/01-qualify-the-demo-substrate/01-VALIDATION.md
@.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md
@.planning/phases/01-qualify-the-demo-substrate/01-01-SUMMARY.md
@.planning/phases/01-qualify-the-demo-substrate/dependency-audit.json
</context>

<interfaces>
- `createBridgeServer(options: { host: "127.0.0.1"; port: number; artifactDir: string }): Promise&lt;BridgeServer&gt;`
- `writeQualificationReport(report: QualificationReport): Promise&lt;string&gt;`
- `readQualificationReport(path: string): Promise&lt;QualificationReport&gt;`
- `scripts/audit-dependencies.mjs --check-lock package-lock.json`

The tracer accepts one `FWAV` version-1 binary `AUDIO_SETTINGS` message. Plan
01-04 extracts and completes the same 32-byte envelope without changing its
layout.
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove Arm modem → binary loopback → canonical report</name>
  <precondition>The active Node version is 22.23.1 and the Plan 01-01 dependency audit passes `--check`.</precondition>
  <reversibility rating="costly">The binary header and JSON persistence seams are consumed by later phase plans, so a change requires coordinated browser, bridge, report, and fixture updates.</reversibility>
  <files>package.json, package-lock.json, apps/modem-ui/index.html, packages/bridge/src/server.ts, tests/skeleton.test.ts</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/dependency-audit.json` — exact direct dependency versions, integrities, engines, and upstream identities.
    - `.planning/phases/01-qualify-the-demo-substrate/01-SKELETON.md` — loopback bridge and canonical JSON architectural contract.
    - `.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md` — 32-byte FWAV envelope, 256 KiB cap, localhost rule, and report location.
    - `.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md` — exact Arm modem copy and honest non-physical evidence language.
  </read_first>
  <behavior>
    - `package.json` pins Node exactly to 22.23.1 and all eight direct packages to the audited exact versions without ranges.
    - Lockfile direct versions and integrities equal the audit before package installation proceeds.
    - A local route exposes semantic `Arm modem`; activation sends an `FWAV` version-1 binary settings message, never base64 or bulk JSON.
    - The bridge binds only `127.0.0.1`, validates same-origin localhost clients and declared length, rejects messages above 256 KiB, and refuses non-loopback configuration.
    - The accepted tracer writes and reads one atomic, schema-versioned per-machine JSON report with `physicalQualification: false`.
  </behavior>
  <action>
Create `package.json` with exact Node engine `22.23.1`, `ws@8.21.1` as the
runtime dependency, and the other seven audited packages as exact development
dependencies. Generate lockfile v3 without lifecycle scripts, run the Plan
01-01 audit utility against it, then install from that verified lockfile. Add
`verify:dependencies` so every aggregate check repeats the direct
version/integrity comparison.

Start red with the skeleton test. Implement the semantic local route and
loopback-only server. For the tracer path, parse the research-defined 32-byte
little-endian header for `AUDIO_SETTINGS`, validate origin, binary kind, magic,
version, declared payload size, and the 256 KiB cap before persisting. Write
the report via temporary file plus atomic rename, read it back for the route,
and mark it explicitly as fixture/loopback evidence that cannot qualify the
physical link. This establishes D-09, D-12, D-13, and D-16 seams without adding
FIPS transport or acoustic reliability.
  </action>
  <verify>
    <automated>node scripts/audit-dependencies.mjs --check-lock package-lock.json &amp;&amp; npm run test:skeleton</automated>
  </verify>
  <acceptance_criteria>
    - The lockfile exactly matches every direct audited version and integrity.
    - The end-to-end test opens an ephemeral loopback server, sends one binary settings frame, and reads the canonical report.
    - Non-loopback configuration, non-local origin, malformed headers, mismatched length, and oversize messages fail closed.
    - The written report remains schema-versioned, atomic, and explicitly non-physical.
  </acceptance_criteria>
  <done>The real local browser/bridge/report path works end-to-end from the verified lockfile.</done>
</task>

<task type="auto">
  <name>Task 2: Complete deterministic Wave 0 commands</name>
  <files>tsconfig.json, vite.config.ts, vitest.config.ts, playwright.config.ts, eslint.config.js, package.json</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/01-VALIDATION.md` — framework, sampling, and command requirements.
    - `package.json` — exact audited dependency set and skeleton scripts from Task 1.
    - `tests/skeleton.test.ts` — first runnable contract that every configuration change must preserve.
  </read_first>
  <action>
Configure strict TypeScript across browser, bridge, scripts, and tests; Vite
and the production bridge on loopback; Vitest for deterministic unit and
integration tests; one Playwright Chromium project for mocked non-hardware
states; and ESLint flat configuration. Define terminating scripts for
`lint`, `typecheck`, `test:unit`, `test:browser`, `test:skeleton`, `build`,
`generate:corpus`, `qualify:fixture`, `qualify:verify`, `test:compose`, and
`check`. The aggregate command must begin with dependency/lockfile verification
and remain independent of microphones, speakers, Docker, TUN, and open-air
codec execution. Configuration anticipates later source paths without requiring
those paths to exist at this task boundary.
  </action>
  <verify>
    <automated>npm run verify:dependencies &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:skeleton &amp;&amp; npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Every named Phase 1 script exists, terminates, and propagates failures.
    - Strict TypeScript covers browser and Node sources.
    - Vite and the bridge default to loopback.
    - The available Wave 0 gate finishes without physical audio, Docker, or TUN.
  </acceptance_criteria>
  <done>Every later deterministic task has a real automated command surface rooted in the verified dependency lock.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| audited metadata → npm lock/install | Registry evidence controls which package archives enter the workspace. |
| browser → local WebSocket bridge | Untrusted frames and origin metadata cross into the local process. |
| bridge → filesystem | Validated tracer evidence becomes canonical JSON. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-SC | Tampering | npm install | high | mitigate | Verify exact direct versions and lockfile integrities against the autonomous audit before npm ci; install only from lockfile. |
| T-01-03 | Spoofing / Elevation | WebSocket server | high | mitigate | Hard-code loopback binding, validate localhost origin, and reject malformed or non-binary frames before processing. |
| T-01-04 | Tampering | tracer report | medium | mitigate | Schema marker, explicit evidence class, atomic rename, and read-after-write integration test. |
</threat_model>

## API Coverage Declaration

Phase 1 exposes no public or remote HTTP API. This plan covers the local
operator route and one binary WebSocket tracer message. Plan 01-04 covers every
Phase 1 FWAV message type; Plan 01-05 covers every codec-adapter and gate
consumer. No untested network-facing endpoint is introduced.

## Artifacts this phase produces

| File | Symbols / contract |
|------|--------------------|
| `package.json` | exact runtime/dependencies and all Phase 1 validation scripts |
| `packages/bridge/src/server.ts` | `createBridgeServer`, tracer parser, report write/read |
| `tests/skeleton.test.ts` | binary loopback-to-report end-to-end test |

<verification>
<automated>npm run verify:dependencies &amp;&amp; npm run lint &amp;&amp; npm run typecheck &amp;&amp; npm run test:skeleton &amp;&amp; npm run build</automated>
</verification>

<success_criteria>
- Exact audit and lockfile remain consistent.
- One production binary loopback/report path is real and tested.
- Wave 0 commands are deterministic and hardware-independent.
</success_criteria>

<output>
Create `.planning/phases/01-qualify-the-demo-substrate/01-02-SUMMARY.md` when done.
</output>
