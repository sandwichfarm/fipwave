---
phase: 01-qualify-the-demo-substrate
plan: 10
type: execute
wave: 8
depends_on:
  - 01-09
files_modified:
  - ".artifacts/qualification/{machine-id-a}.json"
  - ".artifacts/qualification/{machine-id-b}.json"
  - .artifacts/qualification/selection.json
autonomous: false
gap_closure: true
requirements:
  - CODEC-02
  - CODEC-03
  - CODEC-04
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-07
  - DEPLOY-02

must_haves:
  truths:
    - "Both exact laptops run the same checked commit, verified codec assets, built production runner, and one-action Chromium preflight."
    - "The first cold Cyrinx frame is tried A → B and B → A within the immutable window; any miss or expiry immediately runs the fixed audible Quiet fallback."
    - "The selected fixed audible codec carries the required 256-byte and 1,536-byte corpus in both literal directions with measured exact-pair Open air evidence."
    - "Both exact hosts run the least-privilege Docker/TUN procedure without widening authority."
    - "The canonical named verifier either writes cyrinx or quiet from complete exact-pair evidence, or preserves unqualified with the actual reason codes."
  artifacts:
    - path: ".artifacts/qualification/{machine-id-a}.json"
      provides: "Exact laptop A browser, codec, corpus, queue, timing, build, and TUN evidence"
    - path: ".artifacts/qualification/{machine-id-b}.json"
      provides: "Exact laptop B browser, codec, corpus, queue, timing, build, and TUN evidence"
    - path: ".artifacts/qualification/selection.json"
      provides: "Canonical cyrinx, quiet, unqualified, or human-needed decision"
  key_links:
    - from: ".artifacts/qualification/{machine-id-a}.json"
      to: ".artifacts/qualification/selection.json"
      via: "named qualify:verify exact-host merge"
      pattern: "Open air|cyrinx|quiet|unqualified"
    - from: ".artifacts/qualification/{machine-id-b}.json"
      to: ".artifacts/qualification/selection.json"
      via: "the same exact-pair schema and threshold gate"
      pattern: "exact_host|NET_ADMIN|physicalQualification"
---

## Phase Goal

**As a** demo operator, **I want to** qualify the exact two laptops' audible
browser-audio codec path and Docker/TUN substrate, **so that** FIPS integration
begins only with evidence-backed codec and host assumptions.

<objective>
Collect the irreducibly physical browser, acoustic, and Docker/TUN evidence
after the production runner, Quiet fallback, and bounded Cyrinx spike pass
their deterministic tests.

Purpose: Microphones, speakers, room acoustics, Chromium device policy, Docker
engines, and TUN devices cannot be inferred by code or mocks.

Output: Two exact-host reports and one canonical physical selection decision,
or an honest unqualified result that blocks Phase 1.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md
@.planning/phases/01-qualify-the-demo-substrate/01-RUNTIME-GAP-RESEARCH.md
@.planning/phases/01-qualify-the-demo-substrate/01-07-SUMMARY.md
@.planning/phases/01-qualify-the-demo-substrate/01-08-SUMMARY.md
@.planning/phases/01-qualify-the-demo-substrate/01-09-SUMMARY.md
@fixtures/corpus/manifest.json
@codec-assets.lock.json
@docs/qualification-runbook.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify the exact-laptop audible codec and exact-host TUN substrate</name>
  <what-built>A hash-pinned production qualification runner with a real same-origin browser bridge, fixed Quiet audible fallback, tightly bounded Cyrinx C batch path, measured corpus scheduler, canonical report writer/named verifier, and least-privilege Docker/TUN preflight.</what-built>
  <how-to-verify>
    1. On both exact named laptops, check out the same commit and activate Node 22.23.1. Run `npm ci`, `npm run verify:dependencies`, `npm run fetch:codecs`, `npm run fetch:codecs:check`, `npm run build`, `npm run cyrinx:test`, and `npm run check`. Preserve any failure and stop before physical claims.
    2. Run the documented Docker/TUN commands on each host. Keep the exact `/dev/net/tun`, `NET_ADMIN`, `no-new-privileges:true`, non-privileged, no-SYS_ADMIN, `network_mode: none`, IPv6 setup, and owned cleanup contract. Save one `source: exact_host` `TunEvidence` record per laptop; do not widen authority if either host fails.
    3. Place the laptops in the rehearsed open-air arrangement with networking between their qualification pages disabled. Only inside this blocking-human step, enable physical evidence explicitly. On laptop A run `npm run qualification:serve -- --machine-id {machine-id-a} --role A --tun-evidence .artifacts/qualification/tun-exact-host.json --report .artifacts/qualification/{machine-id-a}.json --physical-open-air`; on laptop B use its ID, role B, TUN record, report path, and the same explicit flag. Open each printed `127.0.0.1` URL in Chromium and confirm `/qualification-config` shows the correct read-only identity, role, report target, `source: exact_host` TUN mode, and `Open air` evidence class.
    4. Click `Arm modem` once on each page. Confirm the real bridge is connected and the displayed applied settings show running context, mono 48 kHz capture, AudioWorklet ready, echo cancellation off, noise suppression off, and automatic gain control off.
    5. Start the Cyrinx qualification before its first build. Run the digital gate, then exactly one cold Open air frame A → B and one cold frame B → A. If build/digital work, either cold frame, or the immutable 90-minute window fails, confirm the recorded reason and immediately start/continue the fixed Quiet `audible-7k-channel-0` fallback. Do not extend or retry Cyrinx.
    6. With the surviving codec, run the committed corpus one case at a time: A schedules all A → B cases locally using transmitter `onFinish` plus the fixed guard while B independently receives and records; after the operator observes A's local schedule finish, B schedules all B → A cases the same way while A independently receives. Neither sender waits for remote results, acknowledgements, retries, or ARQ. Keep the sound intentionally audible and retain every missing, duplicate, digest, acquisition, airtime, queue, and discontinuity result.
    7. Stop both runners cleanly and run `npm run qualify:verify -- --machine-a .artifacts/qualification/{machine-id-a}.json --machine-b .artifacts/qualification/{machine-id-b}.json --host-a {machine-id-a} --host-b {machine-id-b} --selection .artifacts/qualification/selection.json`.
    8. Inspect the three JSON files. A selected codec requires exact-pair Open air evidence, at least 19/20 unique byte-perfect exactly-once 256-byte cases and 5/5 1,536-byte cases per direction, cold acquisition in both directions, p95 airtime below one third of the recorded dead-link timeout, zero queue discontinuities, advertised MTU at least 1357, fixed audible profile, and two passing exact-host TUN records. Otherwise retain `unqualified`.
  </how-to-verify>
  <files>.artifacts/qualification/{machine-id-a}.json, .artifacts/qualification/{machine-id-b}.json, .artifacts/qualification/selection.json</files>
  <read_first>
    - `docs/qualification-runbook.md` — exact production, roles, early Cyrinx abandonment, Quiet, corpus, report, and TUN procedure.
    - `codec-assets.lock.json` — immutable codec and license identities.
    - `fixtures/corpus/manifest.json` — immutable required case IDs and digests.
    - `.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md` — visible evidence and nonphysical-language contract.
  </read_first>
  <action>
Perform the eight physical steps using the implemented commands and exact demo
laptops. Record observations produced by the real runner. Preserve any failure,
fallback, or unqualified decision. Do not edit thresholds, asset identities,
reports, browser observations, container authority, Cyrinx deadline, or
evidence class to obtain a pass. Physical evidence must never be inferred from
the deterministic integration test.
  </action>
  <verify>
    <manual-only>Witness both literal open-air directions and both exact-host TUN runs, then run the named canonical verifier in step 7. This trust-establishing checkpoint must never be auto-approved.</manual-only>
  </verify>
  <acceptance_criteria>
    - Both exact laptops use the same commit, asset lock, production runner, and passing applied browser settings.
    - Cyrinx is tried only within the hard window and any miss transitions immediately to Quiet.
    - One fixed intentionally audible codec passes the exact corpus, acquisition, airtime, queue, digest, duplicate, and MTU gates in both directions.
    - Both exact-host reports contain passing least-privilege `TunEvidence`.
    - The canonical selection names `cyrinx` or `quiet`; otherwise Phase 1 remains `unqualified` with actual reason codes.
  </acceptance_criteria>
  <done>The exact hardware either proves one fixed audible codec plus two least-privilege TUN hosts, or Phase 1 remains honestly blocked as unqualified.</done>
  <resume-signal>Type `approved physical qualification` after all eight steps and attach the selection decision, or report the exact failing reason codes.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical room → reports | Ambient sound, echo, loss, replay, duplication, or operator order could corrupt evidence. |
| exact hosts → selection | Wrong build, browser, machine, privilege, or evidence class could be reported as the demo substrate. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-28 | Tampering / Replay | open-air corpus | high | mitigate | Exact manifest, epoch/direction/case/fragment identity, expected/received digest, exactly-once count, measured timing, and witnessed roles. |
| T-01-29 | Spoofing | exact-host reports | high | mitigate | Named hosts/roles, commit, browser, asset lock, local origin, two reports, and canonical verifier. |
| T-01-30 | Elevation of privilege | host TUN | high | mitigate | Exact-host evidence checked against `/dev/net/tun`, NET_ADMIN only, no-new-privileges, no SYS_ADMIN, non-privileged, and isolated networking. |
| T-01-31 | Repudiation | physical approval | high | mitigate | Blocking-human gate, retained failures/reason codes, canonical JSON, and no autonomous approval path. |
</threat_model>

## Decision Traceability

| Decisions | Implemented / physically verified by |
|-----------|----------------------------------------|
| D-01–D-04 | 01-03, 01-07, 01-08; exact browser hardware here |
| D-05–D-08 | 01-05, 01-08, 01-09; exact open-air gate here |
| D-09–D-12 | 01-04, 01-07–01-09; exact measured evidence here |
| D-13–D-16 | 01-06–01-08; exact-host TUN and canonical reports here |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plans | Status |
|--------|----|-----------------------|-------|--------|
| GOAL | — | Exact-laptop audible codec path and Docker/TUN preflight with proven Cyrinx or immediate fallback | 01-07–01-10 | COVERED |
| REQ | CODEC-02 | Time-boxed Cyrinx on both exact laptops/directions | 01-09, 01-10 | COVERED |
| REQ | CODEC-03 | Hard gates and immediate browser-ready fallback | 01-07–01-10 | COVERED |
| REQ | CODEC-04 | Fixed rehearsed intentionally audible profile | 01-07, 01-08, 01-10 | COVERED |
| REQ | WEB-01 | One real browser arm action | 01-07, 01-08, 01-10 | COVERED |
| REQ | WEB-02 | Mono/48 kHz request with processing disabled | 01-08, 01-10 | COVERED |
| REQ | WEB-03 | Applied settings and visible failure | 01-07, 01-08, 01-10 | COVERED |
| REQ | WEB-07 | Same production Chromium route on both laptops | 01-07, 01-08, 01-10 | COVERED |
| REQ | DEPLOY-02 | TUN plus only required capability on exact hosts | 01-06, 01-10 | COVERED |
| RESEARCH | — | Production same-origin route and all FWAV runtime wiring | 01-07 | COVERED |
| RESEARCH | — | Pinned Quiet assets/profile, exclusive audio, fragmentation, and corpus | 01-07, 01-08 | COVERED |
| RESEARCH | — | Pinned portable Cyrinx batch C geometry and early abandonment | 01-09 | COVERED |
| RESEARCH | — | Canonical reports, named verifier, and unmocked browser integration | 01-08 | COVERED |
| CONTEXT | D-01–D-04 | Browser audio qualification | 01-03, 01-07, 01-08, 01-10 | COVERED |
| CONTEXT | D-05–D-08 | Strict Cyrinx selection and immediate fallback | 01-08–01-10 | COVERED |
| CONTEXT | D-09–D-12 | Browser-owned audio and codec-neutral binary boundary | 01-04, 01-07–01-09 | COVERED |
| CONTEXT | D-13–D-16 | Pinned reproducible runtime, least privilege, canonical evidence | 01-06–01-10 | COVERED |

<verification>
<manual-only>Complete the eight-step exact-laptop and exact-host procedure. No fixture, mock, digital round trip, or deterministic runner test can satisfy it.</manual-only>
</verification>

<success_criteria>
- Every Phase 1 implementation gap is closed before this checkpoint begins.
- Only exact-pair Open air and exact-host evidence can produce a selected codec.
- The trust-establishing physical gate is explicitly blocking-human and cannot auto-advance.
</success_criteria>

<output>
Create `.planning/phases/01-qualify-the-demo-substrate/01-10-SUMMARY.md` only after the blocking-human checkpoint is genuinely approved.
</output>
