---
phase: 01-qualify-the-demo-substrate
plan: 06
type: execute
wave: 4
depends_on:
  - 01-04
files_modified:
  - docker/preflight.Dockerfile
  - compose.preflight.yml
  - scripts/preflight-tun.sh
  - scripts/check-compose.mjs
  - tests/tun-preflight.test.ts
  - docs/qualification-runbook.md
autonomous: true
requirements:
  - DEPLOY-02

must_haves:
  truths:
    - "D-13: Docker/TUN qualification inputs are pinned and runnable from repository commands."
    - "D-14: The container receives `/dev/net/tun`, NET_ADMIN, and no-new-privileges only; it is not privileged and receives no alternate inter-laptop path."
    - "D-15: TUN creation, IPv6 configuration, evidence capture, and owned cleanup are proven independently of FIPS."
    - "D-16: Static, fake-lifecycle, and exact-host results share one machine-readable TunEvidence contract plus a concise runbook."
  artifacts:
    - path: "compose.preflight.yml"
      provides: "Least-privilege TUN preflight topology"
    - path: "scripts/preflight-tun.sh"
      provides: "Owned fips-preflight0 create/configure/evidence/cleanup lifecycle"
    - path: "scripts/check-compose.mjs"
      provides: "Fail-closed Compose/inspect validation and TunEvidence output"
    - path: "docs/qualification-runbook.md"
      provides: "Exact Docker Desktop/Linux Engine evidence procedure"
  key_links:
    - from: "compose.preflight.yml"
      to: "scripts/preflight-tun.sh"
      via: "exact TUN device and NET_ADMIN execute the single-interface lifecycle"
      pattern: "/dev/net/tun|NET_ADMIN|fips-preflight0"
    - from: "scripts/check-compose.mjs"
      to: "packages/bridge/src/report.ts"
      via: "stable TunEvidence fields match the canonical report contract"
      pattern: "TunEvidence"
  assumptions:
    - requirement: DEPLOY-02
      category: adjacency
      status: flagged-unverified
      assumption: "Declarative capability/device configuration has no inferred merge semantics; exact-host evidence remains required."
      verification: flagged
    - requirement: DEPLOY-02
      category: empty
      status: resolved
      assumption: "Missing, empty, or null device/capability data is a failed preflight."
      verification: automated
    - requirement: DEPLOY-02
      category: ordering
      status: resolved
      assumption: "Capability and device inputs are exact sets emitted in stable sorted order."
      verification: automated
    - requirement: DEPLOY-02
      category: concurrency
      status: resolved
      assumption: "A run owns only a newly created fips-preflight0 and fails safely if it already exists."
      verification: automated
  prohibitions:
    - status: flagged-unverified
      text: "Must not claim fixture or loopback evidence as physical two-laptop qualification."
    - status: flagged-unverified
      text: "Must not expose the local browser bridge beyond loopback."
    - status: flagged-unverified
      text: "Must not grant privileged mode or SYS_ADMIN to the TUN preflight."
---

## Phase Goal

**As a** demo operator, **I want to** qualify the exact two laptops' audible
browser-audio codec path and Docker/TUN substrate, **so that** FIPS integration
begins only with evidence-backed codec and host assumptions.

<objective>
Implement and deterministically validate the pinned least-privilege Docker/TUN
preflight before exact-host execution.

Purpose: Later FIPS work must inherit a proven TUN lifecycle without privileged
mode, excess capabilities, or an alternate network path.

Output: Pinned image, Compose topology, fail-closed configuration checker,
owned TUN lifecycle, tests, and exact-host runbook.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md
@.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md
@.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md
@.planning/phases/01-qualify-the-demo-substrate/01-04-SUMMARY.md
</context>

<interfaces>
- `TunEvidence` from `packages/bridge/src/report.ts`
- Preflight interface: `fips-preflight0` with one fixed IPv6 test address
- Allowed authority: `/dev/net/tun`, `NET_ADMIN`, `no-new-privileges:true`
</interfaces>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Render Compose through the security checker into TunEvidence</name>
  <files>docker/preflight.Dockerfile, compose.preflight.yml, scripts/check-compose.mjs, tests/tun-preflight.test.ts</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md` — D-13 through D-16.
    - `.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md` — exact Compose controls and inspect evidence.
    - `packages/bridge/src/report.ts` — canonical TunEvidence fields.
  </read_first>
  <behavior>
    - The Linux base is pinned by immutable digest with its readable tag recorded.
    - Static inspection requires exactly `/dev/net/tun`, NET_ADMIN, no-new-privileges, non-privileged mode, no SYS_ADMIN, no host network, and no non-loopback published bridge port.
    - Missing, empty, null, or broader authority fails; input order cannot change the stable result.
  </behavior>
  <action>
Pin the official Linux image by immutable digest and configure Compose per D-13
and D-14. Implement `check-compose.mjs` to validate rendered Compose and inspect
JSON as exact sets, fail closed on absent/null values, reject host networking
or non-loopback ports, and emit stable canonical TunEvidence. Start with tests
for the passing topology and every forbidden/missing authority combination.
  </action>
  <verify><automated>npm run test:compose &amp;&amp; npm run test:unit -- tests/tun-preflight.test.ts</automated></verify>
  <acceptance_criteria>
    - Static tests prove the exact device, capability, privilege, security-option, and network contract.
    - The image reference is immutable.
    - TunEvidence field order and values are deterministic.
  </acceptance_criteria>
  <done>The repository can prove the container authority contract without Docker or a TUN device.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove the owned TUN lifecycle and document exact-host capture</name>
  <files>scripts/preflight-tun.sh, tests/tun-preflight.test.ts, docs/qualification-runbook.md</files>
  <read_first>
    - `.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md` — interface lifecycle, evidence, and host variance.
    - `compose.preflight.yml` and `scripts/check-compose.mjs` — exact configuration/evidence contract from Task 1.
  </read_first>
  <behavior>
    - A fake-ip harness creates, assigns IPv6, raises, reports, and removes only fips-preflight0.
    - An existing interface causes failure without reuse or deletion.
    - Every owned exit path cleans the created interface; failed creation never triggers destructive cleanup.
    - The runbook gives exact build/config/run/inspect/save commands for Docker Desktop and Linux Engine.
  </behavior>
  <action>
Implement the strict-shell D-15 lifecycle with explicit device check, ownership
tracking, one interface/address, detailed link evidence, and cleanup traps.
Test the full lifecycle with fake `ip` and device inputs so the default suite
needs no Docker daemon. Document exact per-host commands and expected D-16
evidence, clearly marking real Docker/TUN execution as the Plan 01-07 hardware
checkpoint rather than inferring it from deterministic tests.
  </action>
  <verify><automated>npm run test:unit -- tests/tun-preflight.test.ts &amp;&amp; npm run test:compose</automated></verify>
  <acceptance_criteria>
    - Tests cover success, collision, device absence, command failure, stable evidence, and owned cleanup.
    - The script never deletes or reuses an interface it did not create.
    - The runbook contains complete exact-host evidence commands without claiming execution.
  </acceptance_criteria>
  <done>The deterministic TUN lifecycle is complete and exact hosts can collect canonical evidence without code changes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host → preflight container | TUN device and network capability cross into the container. |
| preflight script → host namespace | Interface commands must affect only the owned test interface. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-14 | Elevation of privilege | Compose topology | high | mitigate | Exact TUN/NET_ADMIN/no-new-privileges set, immutable image, non-privileged mode, and rejection tests. |
| T-01-15 | Tampering / DoS | interface lifecycle | high | mitigate | Fixed name/address, collision failure, ownership flag, strict shell, and cleanup-path tests. |
| T-01-16 | Spoofing | TunEvidence | medium | mitigate | Rendered-config and inspect validation with stable fail-closed canonical output. |
</threat_model>

## Artifacts this phase produces

| File | Symbols / contract |
|------|--------------------|
| `compose.preflight.yml` | exact least-privilege topology |
| `scripts/preflight-tun.sh` | owned interface lifecycle |
| `docs/qualification-runbook.md` | exact-host commands and expected evidence |

<verification>
<automated>npm run test:compose &amp;&amp; npm run test:unit -- tests/tun-preflight.test.ts</automated>
</verification>

<success_criteria>
- D-13 through D-16 deterministic implementation is complete.
- Static and fake lifecycle tests require no Docker/TUN hardware.
- Only the exact-host execution remains for Plan 01-07.
</success_criteria>

<output>
Create `.planning/phases/01-qualify-the-demo-substrate/01-06-SUMMARY.md` when done.
</output>
