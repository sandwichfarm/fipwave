---
phase: 01-qualify-the-demo-substrate
plan: 07
type: execute
wave: 5
depends_on:
  - 01-05
  - 01-06
files_modified:
  - ".artifacts/qualification/{machine-id-a}.json"
  - ".artifacts/qualification/{machine-id-b}.json"
  - .artifacts/qualification/selection.json
autonomous: false
requirements:
  - CODEC-02
  - CODEC-03
  - CODEC-04
  - WEB-01
  - WEB-02
  - WEB-03
  - WEB-07
  - DEPLOY-02

must_haves:
  truths:
    - "Both exact laptops run the same production artifact, pass one-action applied-audio preflight, and exchange the corpus in both open-air directions."
    - "Cyrinx is selected only within D-05 through D-07 gates; any miss/expiry immediately runs the D-08 Quiet fallback."
    - "The selected profile is fixed, intentionally audible, and supported only by exact-pair Open air evidence."
    - "Each exact host proves D-14 and D-15 TUN operation with the implemented least-privilege topology."
    - "D-16 canonical reports either validate one selection or honestly record unqualified."
  artifacts:
    - path: ".artifacts/qualification/{machine-id-a}.json"
      provides: "Exact laptop A browser, codec, corpus, and TUN evidence"
    - path: ".artifacts/qualification/{machine-id-b}.json"
      provides: "Exact laptop B browser, codec, corpus, and TUN evidence"
    - path: ".artifacts/qualification/selection.json"
      provides: "Canonical Cyrinx, Quiet, or unqualified decision"
  key_links:
    - from: ".artifacts/qualification/{machine-id-a}.json"
      to: ".artifacts/qualification/selection.json"
      via: "qualify:verify merges two named exact-host reports"
      pattern: "Open air|1357|selectedCodec"
    - from: ".artifacts/qualification/{machine-id-b}.json"
      to: ".artifacts/qualification/selection.json"
      via: "same exact-pair merge and threshold gate"
      pattern: "Open air|NET_ADMIN|physicalQualification"
  prohibitions:
    - status: flagged-unverified
      text: "Must not claim fixture or loopback evidence as physical two-laptop qualification."
    - status: flagged-unverified
      text: "Must not expose the local browser bridge beyond loopback."
    - status: flagged-unverified
      text: "Must not grant privileged mode or SYS_ADMIN to the TUN preflight."
---

## Phase Goal

**As a** demo operator, **I want to** qualify the exact two laptops' audible
browser-audio codec path and Docker/TUN substrate, **so that** FIPS integration
begins only with evidence-backed codec and host assumptions.

<objective>
Collect and verify the only irreducibly physical Phase 1 evidence after every
deterministic implementation and test has passed.

Purpose: Exact microphones, speakers, Chromium policies, room acoustics,
Docker engines, and TUN devices cannot be truthfully inferred by automation.

Output: Two exact-host reports and one canonical physical selection decision.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-qualify-the-demo-substrate/01-CONTEXT.md
@.planning/phases/01-qualify-the-demo-substrate/01-RESEARCH.md
@.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md
@.planning/phases/01-qualify-the-demo-substrate/01-05-SUMMARY.md
@.planning/phases/01-qualify-the-demo-substrate/01-06-SUMMARY.md
@fixtures/corpus/manifest.json
@docs/qualification-runbook.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify exact-laptop open-air codec and exact-host TUN evidence</name>
  <what-built>The complete deterministic browser-audio preflight, FWAV/report/corpus contracts, codec-neutral Cyrinx/Quiet qualification gate and console, exact-report verifier, and least-privilege Docker/TUN preflight.</what-built>
  <how-to-verify>
    1. On both exact named laptops, check out the same commit, run `npm ci`, `npm run check`, and the production build; open each local page in Chromium.
    2. Click `Arm modem` once per laptop. Confirm permission, running context, mono/48 kHz-compatible actual settings, processing disabled, worklet ready, and loopback bridge; save each machine report.
    3. Run the documented Docker/TUN build, config, run, and inspect commands on each host. Confirm `/dev/net/tun`, NET_ADMIN only, no SYS_ADMIN, non-privileged mode, no-new-privileges, IPv6 configuration, and owned cleanup.
    4. Start the Cyrinx clock before its first build; run pinned golden vectors, fixture/loopback, then cold Open air `A → B` and `B → A` for the complete corpus. Do not extend or retry the 90-minute Cyrinx gate.
    5. On any Cyrinx failure or expiry, confirm the immutable reason and immediately run the same corpus with the fixed audible Quiet fallback.
    6. Run `npm run qualify:verify -- --machine-a .artifacts/qualification/{machine-id-a}.json --machine-b .artifacts/qualification/{machine-id-b}.json --selection .artifacts/qualification/selection.json`.
    7. Confirm the sound was intentionally audible, both pages remained local/independent, the selected profile advertises at least 1357 bytes, and only exact-pair Open air rows contribute. If neither codec passes, retain `unqualified`.
  </how-to-verify>
  <files>.artifacts/qualification/{machine-id-a}.json, .artifacts/qualification/{machine-id-b}.json, .artifacts/qualification/selection.json</files>
  <read_first>
    - `docs/qualification-runbook.md` — exact browser, corpus, fallback, TUN, and evidence procedure.
    - `fixtures/corpus/manifest.json` — immutable required case IDs/digests.
    - `.planning/phases/01-qualify-the-demo-substrate/01-UI-SPEC.md` — visible proof language and backstops.
  </read_first>
  <action>
Perform only the physical procedure above using the already implemented
commands. Record actual audio, browser, codec, timing, queue, corpus, machine,
and TUN observations in the canonical reports. Preserve every failed or
unqualified result; do not alter thresholds, extend Cyrinx, substitute
fixture/loopback evidence, or infer a host result.
  </action>
  <verify>
    <manual-only>Confirm both open-air directions and both exact-host TUN runs in person, then run the implemented exact-report verifier as step 6 of the physical procedure.</manual-only>
  </verify>
  <acceptance_criteria>
    - Both exact laptops use one build and have passing actual audio reports.
    - The selected audible profile cold-acquires both directions, passes at least 19/20 small and 5/5 large byte-perfect exactly-once cases per direction, meets airtime and queue gates, and advertises at least 1357 bytes.
    - Cyrinx elapsed time is at most 90 minutes; failure routes directly to Quiet without extension.
    - Both TUN reports prove exact least privilege, IPv6 configuration, and cleanup.
    - selection.json validates only Open air exact-pair evidence, or truthfully records unqualified.
  </acceptance_criteria>
  <done>The exact hardware either produces one validated audible selection plus two TUN passes, or Phase 1 remains honestly unqualified.</done>
  <resume-signal>Type `approved physical qualification` after completing all seven steps, or report the failing reason code.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| physical room → reports | Corruption, replay, duplication, or missing sound cases could be mistaken for success. |
| exact hosts → selection | Reports may describe wrong builds, browsers, hosts, privileges, or evidence classes. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-17 | Tampering / Replay | open-air corpus | high | mitigate | Exact manifest, epochs, digests, directions, cold acquisition, delivery counts, and canonical verifier. |
| T-01-18 | Spoofing | exact-host reports | high | mitigate | Named machine/build/browser identities, local endpoints, two reports, and in-person confirmation. |
| T-01-19 | Elevation of privilege | host TUN result | high | mitigate | Exact-host inspect evidence checked against the implemented least-privilege contract. |
</threat_model>

## Decision traceability

| Decisions | Implemented / verified by |
|-----------|---------------------------|
| D-01–D-04 | 01-03; exact hardware verified here |
| D-05–D-08 | 01-05; exact open-air gate verified here |
| D-09–D-12 | 01-02, 01-04, 01-05; exact evidence verified here |
| D-13–D-16 | 01-01, 01-02, 01-06; exact-host evidence verified here |

## Multi-Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plans | Status |
|--------|----|-----------------------|-------|--------|
| GOAL | — | Exact-laptop audible codec path and Docker/TUN preflight | 01-03–01-07 | COVERED |
| REQ | CODEC-02 | Time-boxed Cyrinx on both exact laptops/directions | 01-04, 01-05, 01-07 | COVERED |
| REQ | CODEC-03 | Hard gates and immediate fallback | 01-05, 01-07 | COVERED |
| REQ | CODEC-04 | Fixed intentionally audible profile | 01-05, 01-07 | COVERED |
| REQ | WEB-01 | One browser arm action | 01-02, 01-03, 01-07 | COVERED |
| REQ | WEB-02 | Mono/48 kHz request and processing disabled | 01-03, 01-07 | COVERED |
| REQ | WEB-03 | Applied settings and visible failure | 01-03, 01-07 | COVERED |
| REQ | WEB-07 | Same Chromium implementation both laptops | 01-03, 01-07 | COVERED |
| REQ | DEPLOY-02 | TUN plus only required capability | 01-06, 01-07 | COVERED |
| RESEARCH | — | Binary loopback, corpus, reports, gates, fallback, TUN, validation | 01-01–01-07 | COVERED |
| CONTEXT | D-01–D-04 | Browser audio qualification | 01-03, 01-07 | COVERED |
| CONTEXT | D-05–D-08 | Codec selection gate | 01-04, 01-05, 01-07 | COVERED |
| CONTEXT | D-09–D-12 | Browser/codec boundary and report | 01-02, 01-04, 01-05, 01-07 | COVERED |
| CONTEXT | D-13–D-16 | Repeatable Docker/evidence preflight | 01-01, 01-02, 01-06, 01-07 | COVERED |

## Artifacts this phase produces

| File | Contract |
|------|----------|
| `.artifacts/qualification/{machine-id-a}.json` | exact laptop A evidence |
| `.artifacts/qualification/{machine-id-b}.json` | exact laptop B evidence |
| `.artifacts/qualification/selection.json` | canonical physical decision |

<verification>
<manual-only>Complete the seven-step exact-laptop and exact-host procedure in the checkpoint.</manual-only>
</verification>

<success_criteria>
- All eight phase requirements and D-01 through D-16 are covered.
- Only this final task requires physical/manual verification.
- Selection remains impossible from deterministic fixture/loopback evidence alone.
</success_criteria>

<output>
Create `.planning/phases/01-qualify-the-demo-substrate/01-07-SUMMARY.md` when done.
</output>
