---
phase: 04-prove-the-sound-only-fips-ping
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Dockerfile.bridge
  - packages/bridge/src/proof.ts
  - packages/bridge/src/fips-control-client.ts
  - packages/bridge/test/fips-control-client.test.ts
  - packages/bridge/src/proof-controller.ts
  - packages/bridge/test/proof-controller.test.ts
autonomous: true
requirements: [FIPS-04, DEMO-01, DEMO-02]

must_haves:
  truths:
    - "Per D-01, the proof consumes normal authenticated FIPS peer/link/transport snapshots and never introduces a peer-authentication or encryption bypass."
    - "Per D-08 and D-09, one bounded system `ping -6` executes in Role A's Compose-shared live FIPS network namespace; fixture execution proves orchestration but cannot satisfy physical acceptance."
    - "Per D-10, no ping process starts unless the expected authenticated peer, joined Sound link, current acoustic epoch, current in-band Role B isolation attestation, and configured B `fips0` address agree."
    - "Per D-12 and D-13, proof results retain observed scalar facts, raw command evidence outside the browser projection, and an honest evidence class or `human_needed`."
    - "A bounded Node control client—not a copied `fipsctl` executable—reads only the three allowlisted authoritative snapshot queries over the private local FIPS control socket."
    - "The final bridge image contains Debian `iputils-ping`; FIPS control observation uses the shared socket and adds no second listener or control binary."
  artifacts:
    - path: "packages/bridge/src/fips-control-client.ts"
      provides: "Bounded typed client for the vendored FIPS one-line JSON control protocol"
      exports: ["FipsControlClient", "FipsControlQuery", "createFipsControlClient"]
    - path: "packages/bridge/test/fips-control-client.test.ts"
      provides: "Wire framing, schema, timeout, cleanup, and hostile-response tests"
    - path: "packages/bridge/src/proof.ts"
      provides: "Strict proof snapshot join and bounded result contracts"
      exports: ["joinSoundProof", "parseSoundProofInput", "projectSoundProofResult"]
    - path: "packages/bridge/src/proof-controller.ts"
      provides: "Runner-injected current-state admission and one-ping controller"
      exports: ["ProofController", "createProofController"]
    - path: "packages/bridge/test/proof-controller.test.ts"
      provides: "Fixture-only control snapshot, isolation-gate, process-authority, and fail-closed tests"
  key_links:
    - from: "packages/bridge/src/proof-controller.ts"
      to: "packages/bridge/src/proof.ts"
      via: "strict peer/link/transport/acoustic/isolation join before process launch"
      pattern: "joinSoundProof"
    - from: "packages/bridge/src/proof-controller.ts"
      to: "packages/bridge/src/fips-control-client.ts"
      via: "fresh sequential peers/links/transports batch before each status or ping decision"
      pattern: "query|peers|links|transports"
    - from: "packages/bridge/src/fips-control-client.ts"
      to: "Compose-shared FIPS network namespace"
      via: "private `/run/fips/control.sock` Unix socket plus exact newline-delimited JSON"
      pattern: "createConnection|show_peers|show_links|show_transports"
    - from: "packages/bridge/src/proof.ts"
      to: "Role B configured IPv6 identity"
      via: "expected target equality and Sound-only isolation gate"
      pattern: "targetIpv6|isolation"
---

## Phase Goal

**As a** demo participant, **I want to** reach the otherwise isolated FIPS
node as an authenticated acoustic peer of the wider mesh, **so that** it
returns a real kernel IPv6 echo reply across the same sound link.

<objective>
Establish the production proof tracer from live FIPS control snapshots to one bounded in-namespace kernel ping, while keeping fixture tests and physical acceptance explicitly distinct per D-01 and D-08 through D-13.

Purpose: Prove the authoritative execution and admission architecture before expanding topology, recovery, and presentation.
Output: Bounded FIPS control client, runtime ping tool, strict proof contracts, runner-owned proof controller, and fixture-only orchestration coverage.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-CONTEXT.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-RESEARCH.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-PATTERNS.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-VALIDATION.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-06-SUMMARY.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-07-SUMMARY.md

<interfaces>
- `resolveDemoConfig(input?: string, overrides?: unknown): DemoConfig` supplies the expected public peer and fixed role identity.
- Vendored FIPS at `fc8ebd5` uses an unversioned newline-delimited JSON protocol: one UTF-8 request line, one UTF-8 response line, then close. Exact read-only requests are `{"command":"show_peers"}\n`, `{"command":"show_links"}\n`, and `{"command":"show_transports"}\n`; successful responses are exact `{"status":"ok","data":...}` envelopes and daemon failures are exact `{"status":"error","message":"..."}` envelopes.
- On the Linux Docker target, `node.control.socket_path` is a Unix socket; `/run/fips/control.sock` is selected through private role config and a Compose-shared runtime volume. `127.0.0.1:21210` is the vendored Windows-only implementation and must not be invented as a Linux listener.
- Peer `link_id`, link `transport_id`, and transport `transport_id` are the authoritative join keys. `show_peers.data.peers[]`, `show_links.data.links[]`, and `show_transports.data.transports[]` are strict query-specific schemas.
- Phase 3's Sound transport snapshot distinguishes worker-up from `acoustic_ready` and publishes its current epoch.
- `createFipsControlClient` receives an injected stream-transport factory and clock in tests. `createProofController` receives that typed client plus acoustic-status, isolation-attestation, clock, and `execFile` seams; Plan 04-03 constructs both production instances in `runner.ts`.
</interfaces>
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace current authenticated Sound facts to one kernel ping</name>
  <precondition>Phase 3 readiness/disarm artifacts `apps/modem-ui/src/acoustic-session-adapter.ts` and `vendor/fips/src/transport/sound/mod.rs` exist and their focused tests pass.</precondition>
  <files>packages/bridge/src/fips-control-client.ts, packages/bridge/test/fips-control-client.test.ts, packages/bridge/src/proof.ts, packages/bridge/src/proof-controller.ts, packages/bridge/test/proof-controller.test.ts</files>
  <behavior>
    - The client accepts only enum values `peers`, `links`, and `transports`, maps them internally to the three parameterless `show_*` commands, and cannot send caller-provided command text or params.
    - Each query opens one injected stream, writes one exact newline-terminated JSON request of at most 64 bytes, half-closes its write side, accepts exactly one newline-terminated response of at most 65,536 bytes, then destroys/closes the stream.
    - Connect, read, and total query deadlines are 500 ms, 1,500 ms, and 2,000 ms respectively; one client permits only one in-flight query, performs no retry, and abort/close rejects queued work and destroys the active stream.
    - Only exact `status: ok` plus the query-specific `data.peers[]`, `data.links[]`, or `data.transports[]` schema succeeds. Exact `status: error` is mapped to a bounded daemon-error code; EOF-before-newline, extra frames/keys, malformed JSON, oversized data, wrong field types, disconnects, or stale batch timing map to safe closed reasons without surfacing raw daemon text.
    - A current expected peer connected through `sound`, its active link, the same Sound transport ID, matching acoustic epoch, a current accepted isolation-attestation result, and configured B target produce `pingReady: true`.
    - Any stale timestamp/epoch, wrong peer, non-Sound peer/link, broken ID join, absent/failed isolation result, or target mismatch produces one bounded blocking reason and invokes no ping process.
    - A ready request invokes exactly `/usr/bin/ping -6 -n -c 1 -W 15 &lt;configured-b-ipv6&gt;` through an argument array with a 20-second process timeout and bounded output buffer in the bridge/FIPS shared network namespace.
    - Exit 0/1/2 or runner failure retains raw stdout/stderr in the structured artifact but projects only bounded exit, sequence, latency, loss, and safe-reason scalars.
    - Fixture inputs and injected process results remain `Fixture`; missing matching physical records produce `human_needed` and never an Open-air pass.
  </behavior>
  <action>
Write `fips-control-client.test.ts` first from
`vendor/fips/src/control/protocol.rs`, `vendor/fips/src/control/mod.rs`,
`vendor/fips/src/control/queries.rs`, and
`vendor/fips/docs/reference/control-socket.md`. Implement
`FipsControlQuery` as a fixed enum/union mapped internally to only
`show_peers`, `show_links`, and `show_transports`; do not expose a generic
command or params method. Implement the current `fc8ebd5` unversioned
one-request/one-response NDJSON framing over Node's Unix-socket stream API with
constants `MAX_REQUEST_BYTES=64`, `MAX_RESPONSE_BYTES=65536`,
`CONNECT_TIMEOUT_MS=500`, `READ_TIMEOUT_MS=1500`, and
`TOTAL_TIMEOUT_MS=2000`. Serialize exactly one command object plus LF, accept
exactly one LF-terminated response, and reject partial EOF, a second frame,
trailing non-whitespace, oversized input, invalid UTF-8/JSON, unknown envelope
keys/status, missing success data, query-shape mismatch, and unsafe numeric or
string fields. Validate the current peer/link/transport row fields needed by
the join, including Sound `stats.worker_up`, `stats.acoustic_ready`, and
`stats.epoch`. Map transport/connect/timeout/abort, daemon-error, and protocol
failures to stable bounded codes while retaining bounded diagnostic detail
outside public status. Serialize queries through one in-flight queue with no
retry; implement `AbortSignal` plus idempotent `close()` that rejects queued
work and destroys the active socket. Inject a stream factory and clock so
fragmented reads, disconnects, timeouts, stale batches, malformed/oversized
frames, daemon errors, and cleanup are deterministic in tests.
Implement exact-schema immutable proof inputs and joins in
`packages/bridge/src/proof.ts`; allowlist peer, link, transport, acoustic,
isolation, target, timestamp, counter, and evidence fields, rejecting unknown
or inconsistent values. Implement `ProofController` around the injected typed
`FipsControlClient`, acoustic-status, isolation-attestation, clock, and
argument-array process seams. Its tracer obtains a fresh sequential
peers/links/transports batch, records batch start/completion, rejects a batch
that spans more than 2,000 ms or is no longer current, requires an accepted current
isolation result, and invokes exactly one system ping only after every gate
agrees. Reuse normal FIPS authentication and link encryption per D-01,
preserve the Phase 3 authorization boundary per D-02, and classify the
automated tracer as Fixture per D-09 and D-13.
  </action>
  <verify>
    <automated>npx vitest run packages/bridge/test/fips-control-client.test.ts packages/bridge/test/proof-controller.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Tests observe exact three-command allowlisting, NDJSON bytes, one-response close semantics, query-specific schemas, and `/usr/bin/ping -6` argument arrays, timeout, count, target, and buffer bounds.
    - Stale, partial, malformed, oversized, wrong-schema, daemon-error, disconnected, timed-out, aborted, and concurrent-query cases all fail closed with stable safe codes and release timers/listeners/sockets.
    - Every D-10 prerequisite has a named rejection case that proves the ping runner was not called.
    - Raw stdout/stderr, secrets, packet bytes, and full control documents never enter the public projection.
    - A Fixture success cannot satisfy DEMO-01 or DEMO-02 physical acceptance.
  </acceptance_criteria>
  <done>The production tracer obtains authoritative snapshots through a bounded source-compatible Node control client and can execute one gated kernel ping, while hostile protocol cases fail closed and deterministic tests remain honestly Fixture-only.</done>
</task>

<task type="auto">
  <name>Task 2: Put the authoritative system ping in the runner image</name>
  <files>Dockerfile.bridge</files>
  <action>Install Debian `iputils-ping` in the existing bridge runtime image because the runner-owned controller executes inside the Compose-shared FIPS network namespace. Keep the FIPS image unchanged and do not build or copy `fipsctl`; the Node client reads the daemon's shared Unix socket directly. Preserve the non-root-capability posture and existing bridge entrypoint.</action>
  <verify>
    <automated>docker build -f Dockerfile.bridge -t fipwave-bridge:phase4 . &amp;&amp; docker run --rm --entrypoint sh fipwave-bridge:phase4 -ec 'test -x /usr/bin/ping'</automated>
  </verify>
  <done>The runner image contains the fixed system ping binary without adding a second FIPS control executable or listener.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Runner proof controller → shared FIPS network namespace | Validated role/target values become direct system-process arguments inside the Compose-owned namespace. |
| Shared FIPS Unix socket → Node control client | The socket has local filesystem authorization but no protocol authentication; every frame remains untrusted until byte, envelope, query-schema, freshness, and cross-snapshot validation pass. |
| Fixture/physical evidence → acceptance result | Nonphysical data must not be promoted to a physical Sound proof. |

## ASVS L1 Applicability and Controls

| Category | Applies | Task/test control |
|---|---|---|
| V2 Authentication | Yes | The control socket has local filesystem/group authorization only; Task 1 separately requires the expected normally authenticated FIPS peer/Sound join and rejects wrong/unconnected identities. |
| V3 Session/Replay | Yes | One in-flight fresh snapshot batch plus current epoch/timestamp/isolation-result gates prevent stale control state from authorizing process invocation. |
| V4 Authorization/Topology | Yes | A private fixed socket path, three-query allowlist, Role A-only controller admission, and configured B target are tested; no arbitrary control command exists. |
| V5 Input/Process Invocation | Yes | Exact NDJSON/envelope/query schemas, 64/65,536-byte caps, deadlines, safe errors, and fixed `execFile` arguments are asserted. |
| V6 Identity/Evidence Integrity | Yes | Existing FIPS identity/link encryption remains authoritative; evidence class and raw/public separation are tested. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01-01 | Spoofing | authenticated peer proof | high | mitigate | Require expected npub, connected state, Sound transport, and peer/link/transport ID join from current typed control snapshots per D-01/D-10. |
| T-04-01-02 | Tampering | ping command arguments | high | mitigate | Derive the IPv6 target from validated role config and use the fixed system binary through `execFile` argument arrays with no shell. |
| T-04-01-03 | Repudiation | evidence disposition | high | mitigate | Persist evidence class, process exit, timestamps, and raw command evidence; return `human_needed` until the physical open-air observation required by D-13 is recorded. |
| T-04-01-04 | Information Disclosure | public proof projection | medium | mitigate | Allowlist bounded scalars and public identities; retain nsecs, raw output, packet bytes, and raw control JSON outside browser status. |
| T-04-01-05 | Denial of Service | control socket/ping process | high | mitigate | One in-flight query, 64/65,536-byte caps, connect/read/total deadlines, abort cleanup, one ping attempt, bounded process output, and no retry loop. |
| T-04-01-06 | Elevation of Privilege | FIPS control command surface | high | mitigate | Private exact Unix-socket path and fixed query enum expose only peers/links/transports; no arbitrary command, params, shell, or TCP listener exists. |
| T-04-01-SC | Tampering | package/tool supply | low | accept | No npm/crates package is added; the client uses Node built-ins and iputils-ping comes from the pinned Debian image source. |
</threat_model>

<verification>
- Run the focused control-client/controller TypeScript fixture tests.
- Build the bridge image and verify the system `ping` executable in its final stage.
- Confirm every fixture result remains labelled `Fixture` or `human_needed`.
</verification>

<success_criteria>
- Normal FIPS control state, not logs or browser inference, owns peer/link readiness.
- A single bounded real `ping -6` command is the only acceptance execution path.
- All prerequisite failures block before process launch and expose a bounded reason.
</success_criteria>

<output>
Create `.planning/phases/04-prove-the-sound-only-fips-ping/04-01-SUMMARY.md` when done.
</output>
