---
phase: 04-prove-the-sound-only-fips-ping
plan: "02"
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - packages/bridge/src/demo-config.ts
  - packages/bridge/test/demo-config.test.ts
  - packages/bridge/src/runner.ts
  - tests/production-runner.test.ts
  - packages/bridge/src/isolation-attestation.ts
  - packages/bridge/test/isolation-attestation.test.ts
  - compose.fips.yml
  - scripts/fips-compose-smoke.mjs
  - tests/fips-compose.test.mjs
autonomous: true
requirements: [DEPLOY-03, DEPLOY-04, DEPLOY-05, CONFIG-04]

must_haves:
  truths:
    - "Per D-04, generated Role B FIPS config contains exactly one Sound transport and exactly one demo peer address, which uses Sound to reach configured Role A."
    - "Per D-04, live Role B `show_transports` cardinality plus Docker inspection—not YAML text alone—proves exactly one usable Sound transport with current type/state/worker/readiness and expected peer/link association."
    - "Per D-05, browser and FIPS bridge endpoints remain local loopback/service-namespace boundaries and cannot carry packets between laptops."
    - "Per D-06 and CONFIG-04, Role A enables Sound plus the existing outbound-only UDP client posture for optional wider-mesh participation without adding a required operator parameter or another address for B."
    - "Per D-07, fixed public identities and deterministic `fips0` addresses remain available to proof code while nsecs stay only in mode-0600 generated config."
  artifacts:
    - path: "packages/bridge/src/demo-config.ts"
      provides: "Single immutable role-specific transport and proof-target authority"
    - path: "packages/bridge/src/runner.ts"
      provides: "Secret-bearing A/B FIPS configuration renderer"
      exports: ["renderFipsConfig"]
    - path: "scripts/fips-compose-smoke.mjs"
      provides: "Live Role B transport-cardinality and local-topology assertions"
    - path: "packages/bridge/src/isolation-attestation.ts"
      provides: "Bounded IPv6 UDP challenge/attestation codec and Role B namespace-sharing responder"
      exports: ["IsolationChallenge", "IsolationAttestation", "createIsolationResponder", "parseIsolationAttestation"]
    - path: "tests/fips-compose.test.mjs"
      provides: "Mutation coverage for alternate transports, ports, namespaces, and capabilities"
  key_links:
    - from: "packages/bridge/src/demo-config.ts"
      to: "packages/bridge/src/runner.ts"
      via: "one role-aware private config and allowlisted public projection"
      pattern: "resolveDemoConfig|renderFipsConfig"
    - from: "packages/bridge/src/runner.ts"
      to: "scripts/fips-compose-smoke.mjs"
      via: "generated config is checked against live bounded Node control-client and Docker runtime facts"
      pattern: "show_transports|NetworkMode"
    - from: "packages/bridge/src/isolation-attestation.ts"
      to: "Role B fips0 fixed proof port"
      via: "exact-schema nonce-bound UDP response over the authenticated FIPS/Sound path"
      pattern: "createSocket|challenge|snapshotDigest"
    - from: "compose.fips.yml"
      to: "browser"
      via: "loopback-only browser publication while FIPS shares the bridge service namespace"
      pattern: "127\\.0\\.0\\.1|network_mode"
---

## Phase Goal

**As a** demo participant, **I want to** reach the otherwise isolated FIPS
node as an authenticated acoustic peer of the wider mesh, **so that** it
returns a real kernel IPv6 echo reply across the same sound link.

<objective>
Make A/B role selection produce and prove the exact transport topology required by D-04 through D-07: B is Sound-only at configuration and runtime, while A is the participant/wider-mesh side without weakening the local bridge boundary.

Purpose: Ensure the real ping cannot succeed through an unintended FIPS or browser path.
Output: Role-specific config contracts, secret-safe rendering, live transport-cardinality inspection, and topology mutation tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-CONTEXT.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-RESEARCH.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-PATTERNS.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-VALIDATION.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-01-SUMMARY.md

<interfaces>
- `DemoConfig.fips` currently contains `linkMtu` and `expectedPeerPublicKey`; extend this authority with deterministic Role B target/address and role transport policy.
- `toPublicDemoConfig(config)` must remain an explicit allowlist and expose only the fixed proof port/bounds needed by both roles; it must never spread private identity fields.
- Vendored `UdpConfig` supports `outbound_only: true` and `accept_connections: false`; this is the chosen Role A optional wider-mesh posture.
- `assertFipsRuntimeInspect(inspected)` and `assertFipsTunRuntime(output, role)` already own effective Compose/TUN assertions.
</interfaces>
</context>

<assumption_delta_decision>
  <noun>transport kind</noun>
  <decision>no-change</decision>
  <rationale>The existing FIPS transport enum, peer-address abstraction, and `show_transports` snapshot are already generalized; Phase 4 only constrains Role B cardinality and selects existing Sound/UDP variants.</rationale>
</assumption_delta_decision>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Generate exact A participant and B Sound-only configurations</name>
  <files>packages/bridge/src/demo-config.ts, packages/bridge/test/demo-config.test.ts, packages/bridge/src/runner.ts, tests/production-runner.test.ts</files>
  <behavior>
    - Input role `b` resolves to one Sound transport policy, fixed A expected peer, deterministic B `fips0` target, and no wider-mesh policy.
    - Rendered Role B YAML has one `transports.sound` instance and its sole peer has one Sound address; no defaults can instantiate another transport.
    - Input role `a` resolves to Sound plus one existing UDP instance configured `outbound_only: true`, `accept_connections: false`, and not advertised; the configured B peer still has only its Sound address.
    - The public projection exposes only role, public identity/peer, deterministic target, transport-kind status, and local proof URL; no nsec or secret-bearing config text is present.
    - Both roles derive one fixed B `fips0` proof port and bounded attestation limits from role selection; no host-published port or operator parameter is introduced.
  </behavior>
  <action>
Extend `resolveDemoConfig` rather than creating another topology authority.
Represent the transport policy as exact immutable role-derived data: Role B is
one Sound instance; Role A is Sound plus the vendored outbound-only UDP client
posture so it can participate through an environment-provided upstream without
opening a known inbound port. Do not add an upstream address to the A↔B peer
and do not add a required role argument or host-discovery parameter. Extend
the config contract used by `renderFipsConfig` so every transport section is
explicit, B cannot inherit a default, normal `auto_connect`/`auto_reconnect`
stays enabled for the Sound peer, and only the private generated file contains
nsec per D-01, D-04, D-06, and D-07. Add fixed proof-port and datagram,
challenge, timeout, retry, rate, replay, and freshness bounds to the same
authority. Test exact role policy and the allowlisted public projection.
  </action>
  <verify>
    <automated>npx vitest run packages/bridge/test/demo-config.test.ts tests/production-runner.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Role B config contains one transport instance and one Sound peer address.
    - Role A config contains Sound plus outbound-only non-advertised UDP, while B's peer address remains Sound-only.
    - Role is the only required selection input.
    - Public config and runner errors contain no nsec or raw secret-bearing YAML.
  </acceptance_criteria>
  <done>One configuration authority deterministically produces A's participant posture and B's isolated Sound-only posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove B isolation and local-only bridge boundaries from live runtime facts</name>
  <precondition>Plan 04-01 provides the compiled bounded Node FIPS control client, and Compose mounts the private `/run/fips/control.sock` volume into the bridge namespace.</precondition>
  <files>packages/bridge/src/isolation-attestation.ts, packages/bridge/test/isolation-attestation.test.ts, compose.fips.yml, scripts/fips-compose-smoke.mjs, tests/fips-compose.test.mjs</files>
  <behavior>
    - Role B live bounded Node control-client snapshots have usable transport cardinality one, type `sound`, current Up state, worker-up/readiness true, and a transport/link association to the expected authenticated A peer; unavailable/failed/mismatched state blocks attestation.
    - Live Docker inspection still proves no FIPS published port, bridge browser publication on host loopback only, shared service namespace, non-privileged containers, exact capability set, and TUN device.
    - Role A permits its explicit outbound-only UDP plus Sound and still presents B as a Sound peer.
    - Mutating config or inspection evidence to add an alternate transport, wildcard browser publication, host networking, extra capability, or FIPS port makes the deterministic test fail.
    - A 32-byte cryptographically random challenge yields one exact-schema response no larger than 1024 bytes binding the echoed challenge, expected B npub, B target, run/build, acoustic epoch/bounded settings identifier, fresh snapshot time, one usable Sound transport, expected peer/link association, and canonical SHA-256 snapshot digest.
    - The responder accepts at most two sends of one challenge, a 45-second attempt timeout, six requests per minute, and a 32-entry/120-second replay cache; wrong source/session, oversize, timeout, mismatch, replay, or rate excess fails closed.
  </behavior>
  <action>
Create the strict UDP challenge/attestation codec and Role B responder using
Node `dgram`/`crypto` built-ins only. Bind the responder to the deterministic B
`fips0` IPv6 address and fixed proof port after the expected normal FIPS Sound
peer is authenticated; obtain every response field from one current local
control/acoustic/Compose snapshot and canonicalize/digest only the bounded
projection. The FIPS link supplies channel authentication/integrity; the
one-use random challenge supplies request freshness. Do not add application
signing, a browser/LAN endpoint, or a file-copy record.

Extend the existing smoke authority, not daemon log parsing. Execute a
purpose-specific in-container Node probe from the bridge container that imports
the compiled `createFipsControlClient`, uses only the fixed
`/run/fips/control.sock` path, performs the allowlisted `peers`, `links`, and
`transports` queries, closes the client, and emits one bounded canonical
snapshot for the smoke parent to strict-parse. Invoke it with `docker compose
exec -T bridge node --input-type=module -e <fixed-source>` through an argument
array: no caller-supplied command text, no copied binary, no TCP listener, and
no host socket access. Assert exact current Sound type/state/worker/readiness
and expected peer/link association beside the
existing `docker inspect`, `fips0`, process, capability, namespace, and port
checks. Keep browser publication on `127.0.0.1`, FIPS un-published, and make
the responder's UDP port reachable only on `fips0` inside the existing shared
service network namespace—never in Compose `ports`—per D-05. Add hostile
datagram/replay/rate tests and mutations for every forbidden runtime topology;
keep smoke cleanup project-owned.
  </action>
  <verify>
    <automated>npx vitest run packages/bridge/test/isolation-attestation.test.ts &amp;&amp; node --test tests/fips-compose.test.mjs &amp;&amp; node scripts/check-compose.mjs --fips-source</automated>
  </verify>
  <acceptance_criteria>
    - B isolation needs both rendered-config and live snapshot/inspect evidence.
    - Current Sound usable-state and expected peer/link association are mandatory response fields; unavailable/failed values block the response.
    - The fixed proof UDP port is internal to `fips0`/the shared namespace and absent from host publications.
    - A's optional UDP instance cannot become an address for B and cannot accept inbound new peers.
    - No LAN browser, bridge, host-network, or FIPS-published-port attestation path is added.
    - Every mutation fails with one bounded actionable assertion.
  </acceptance_criteria>
  <done>Runtime evidence proves Role B has no usable FIPS transport except Sound and local bridge endpoints cannot bypass the acoustic hop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Role/config input → secret FIPS YAML | A single literal role determines private identity and transport policy. |
| Requested Compose topology → effective runtime | Source config alone cannot prove effective ports, namespaces, privileges, or transports. |
| A fips0 UDP challenge → B responder | Untrusted datagrams cross only after normal FIPS/Sound authentication and must be bounded/current/replay-safe. |

## ASVS L1 Applicability and Controls

| Category | Applies | Task/test control |
|---|---|---|
| V2 Authentication | Yes | Task 2 emits an attestation only for the expected authenticated A peer joined through Sound; wrong-peer fixtures reject. |
| V3 Session/Replay | Yes | One-use random challenge, current epoch/settings identifier, freshness window, replay cache, and timeout/retry bounds have hostile tests. |
| V4 Authorization/Topology | Yes | Task 1 derives role policy; Task 2 enforces B-only responder, exact one usable Sound transport, and no host-published proof port. |
| V5 Input/Process Invocation | Yes | Exact UDP schema/size/source/port/rate validation, the fixed argument-array in-container Node probe, and strict control/Compose snapshot parsers are mutation-tested. |
| V6 Identity/Evidence Integrity | Yes | FIPS Noise/link encryption authenticates the channel; challenge echo and canonical SHA-256 snapshot digest bind response identity/evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-02-01 | Elevation of Privilege | Role B transport policy | high | mitigate | Exact B config plus live show_transports cardinality one/type Sound assertion per D-04. |
| T-04-02-02 | Spoofing | role selection | high | mitigate | Accept only literal a/b through the existing resolver and derive identities/peers/target internally. |
| T-04-02-03 | Information Disclosure | public config | high | mitigate | Explicit public allowlist; nsecs remain in mode-0600 generated config only per D-07. |
| T-04-02-04 | Tampering | Docker topology | high | mitigate | Inspect effective namespace, publications, privileges, capabilities, TUN, process, and transport snapshots; mutation-test every boundary. |
| T-04-02-05 | Denial of Service | optional Role A UDP | medium | accept | Outbound-only ephemeral UDP is bounded by existing FIPS transport behavior and is not required for the A↔B acceptance path. |
| T-04-02-SC | Tampering | dependency surface | low | accept | Uses existing vendored Sound/UDP abstractions and current Docker tooling; no package install is added. |
</threat_model>

<verification>
- Run focused config/runner and Compose mutation tests.
- Build/start each role and run the live smoke assertions when Docker is available.
- Confirm role B live transport count is one and role A's B peer address is Sound-only.
</verification>

<success_criteria>
- Configuration and runtime evidence agree on B's Sound-only cardinality.
- Role A has a cross-platform existing FIPS participant transport without any new required operator input.
- Browser/bridge ports remain local and cannot serve as another inter-laptop path.
</success_criteria>

<output>
Create `.planning/phases/04-prove-the-sound-only-fips-ping/04-02-SUMMARY.md` when done.
</output>
