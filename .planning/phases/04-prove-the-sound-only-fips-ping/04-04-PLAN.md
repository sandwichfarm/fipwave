---
phase: 04-prove-the-sound-only-fips-ping
plan: "04"
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - apps/modem-ui/src/proof-state.ts
  - apps/modem-ui/src/proof-state.test.ts
  - apps/modem-ui/src/main.ts
  - apps/modem-ui/src/style.css
  - apps/modem-ui/e2e/sound-proof.spec.ts
autonomous: true
requirements: [DEMO-03, DEMO-04]

must_haves:
  truths:
    - "Per D-12 and the approved 04-UI-SPEC, the diagnostic page renders a `FIPS proof status` card from strict structured facts and never infers peer, isolation, link, ping, or evidence success."
    - "The card renders the prescribed fields in order, including evidence class, authenticated peer, active Sound link, B isolation, current acoustic epoch, complete-packet/acoustic/fragment/integrity/retry counters, and ping readiness."
    - "Role A alone gets `Run sound-only ping`, enabled only for a fresh Ready snapshot; both roles get `Refresh proof status`, and Role B gets the exact isolated-node explanation."
    - "Fixture/Loopback reply, Open-air reply, `human_needed`, failure, blocked, running, degraded/reconnecting, and error states use the approved copy/color/authority contract."
    - "All 24/24 UI state considerations remain explicit: empty/loading/error/populated/partial/overflow/zero-one-many/long-text behavior is tested without adding Phase 5 no-scroll/presenter work."
  artifacts:
    - path: "apps/modem-ui/src/proof-state.ts"
      provides: "Strict parser/reducer and current outcome invalidation"
      exports: ["ProofState", "parseProofSnapshot", "reduceProofState"]
    - path: "apps/modem-ui/src/main.ts"
      provides: "Semantic proof card, role-aware controls, and bounded outcome rendering"
    - path: "apps/modem-ui/src/style.css"
      provides: "Approved diagnostic-card states, responsive wrapping, focus, and busy treatment"
    - path: "apps/modem-ui/e2e/sound-proof.spec.ts"
      provides: "Production-page UI contract and 320px overflow/accessibility coverage"
  key_links:
    - from: "packages/bridge/src/server.ts"
      to: "apps/modem-ui/src/proof-state.ts"
      via: "same-origin GET /proof-status and POST /proof-ping bounded JSON"
      pattern: "/proof-status|/proof-ping|parseProofSnapshot"
    - from: "apps/modem-ui/src/proof-state.ts"
      to: "apps/modem-ui/src/main.ts"
      via: "reducer-owned readiness, evidence disposition, counters, and outcome"
      pattern: "reduceProofState"
    - from: "apps/modem-ui/src/main.ts"
      to: "Run sound-only ping control"
      via: "native disabled/busy state from current Role A proof readiness only"
      pattern: "disabled|aria-busy"
---

## Phase Goal

**As a** demo participant, **I want to** reach the otherwise isolated FIPS
node as an authenticated acoustic peer of the wider mesh, **so that** it
returns a real kernel IPv6 echo reply across the same sound link.

<objective>
Implement the approved Phase 4 diagnostic proof surface exactly, lifting all UI truth, interaction, accessibility, responsive, and evidence-state criteria into executable tests.

Purpose: Let an operator visibly inspect the real proof sources and run one gated ping without confusing local readiness or nonphysical evidence with success.
Output: Strict proof reducer, `FIPS proof status` card, role-aware proof controls, bounded ping outcome, styles, and production-page acceptance tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-CONTEXT.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-UI-SPEC.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-PATTERNS.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-VALIDATION.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-03-SUMMARY.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-07-SUMMARY.md

<interfaces>
- `refreshBridgeState(): Promise&lt;void&gt;` is the existing guarded local diagnostic refresh pattern.
- `BridgeSnapshot` and `AcousticPublicStatus` use exact-schema parsers and immutable projections; `ProofState` must follow the same fail-closed pattern.
- Plan 04-03 supplies exact same-origin `GET /proof-status` and `POST /proof-ping` on the existing listener; the UI displays their safe projection and never raw control/attestation/ping output.
- Existing semantic DOM helpers, `.card`, `.operator-card`, `.console-grid`, `dl`, `.measurements`, native button focus, and responsive diagnostic layout remain the design system.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reduce strict current proof snapshots into every approved UI state</name>
  <files>apps/modem-ui/src/proof-state.ts, apps/modem-ui/src/proof-state.test.ts</files>
  <behavior>
    - Exact valid snapshots represent Loading, blocked, Ready, Running, nonphysical reply, Open-air reply, human-needed, ping failed, degraded/reconnecting, and Error.
    - A bridge/audio-ready snapshot without authoritative peer/link/isolation facts remains blocked with `Waiting for authenticated Sound peer`.
    - Fixture or Loopback exit 0 remains a nonphysical reply; only matching two-machine Open-air evidence receives Open-air success.
    - Disarm, epoch change, peer/link/Sound/heartbeat/isolation loss, stale timestamp, or target mismatch clears the current outcome and disables ping.
    - Missing counters remain unavailable, long safe reasons are capped, older snapshots are ignored, and a fresh post-result snapshot is required before another ping.
  </behavior>
  <action>
Create `proof-state.test.ts` first from the complete state and interaction
tables in 04-UI-SPEC. Implement an exact-key parser plus immutable reducer that
accepts only the Plan 04-03 safe projection. Preserve previous-epoch observed
counters with an explicit label while clearing their current authority.
Enforce D-12/D-13 evidence and inference boundaries in the reducer, including
the exact nonphysical, Open-air, human-needed, blocked, degraded, and failure
copy. Make Role A/Role B control availability derived from validated role and
current state, not DOM conditions.
  </action>
  <verify>
    <automated>npx vitest run apps/modem-ui/src/proof-state.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All UI-SPEC state rows and evidence classes have named reducer tests.
    - All six invalidation categories clear current outcome/readiness.
    - Partial/unknown facts cannot default to ready, success, or numeric zero.
    - One latest bounded attempt is retained; no attempt history/tally is introduced.
  </acceptance_criteria>
  <done>A pure tested reducer owns every approved proof state and makes stale or inferred success impossible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the exact proof card, controls, outcome, and responsive semantics</name>
  <files>apps/modem-ui/src/main.ts, apps/modem-ui/src/style.css, apps/modem-ui/e2e/sound-proof.spec.ts</files>
  <behavior>
    - The page heading is `Sound-only FIPS proof`; existing metadata, runtime status, operator controls, and diagnostic cards remain present and scroll normally.
    - `FIPS proof status` appears immediately after `Bridge and FIPS transport`, uses a semantic `dl`, and renders every prescribed label/value in order.
    - Role A shows Refresh plus native primary Run; Role B shows only Refresh plus the exact isolated-node explanation. Running sets native disabled and `aria-busy`.
    - Refresh calls exact same-origin `GET /proof-status`; Role A Run calls exact same-origin `POST /proof-ping` with `Content-Type: application/json` and exact `{}` body. No alternate URL/listener is used.
    - The proof card has exactly one proof live region: polite for loading/ready/running/human-needed and assertive for ping failure/error; counters are not individually announced.
    - A 320px viewport has no page-level horizontal overflow; IDs/IPv6/reasons/counters wrap, existing tables keep contained overflow, and desktop diagnostics remain scrollable.
  </behavior>
  <action>
Write the production-page Playwright contract before DOM wiring. Change the
heading and insert the proof card/control additions exactly where 04-UI-SPEC
specifies, preserving every existing diagnostic card. Implement one guarded
proof refresh against exact same-origin `GET /proof-status` and one Role A
ping action against exact same-origin `POST /proof-ping` with the fixed empty
JSON contract;
disable refresh while ping is running, clear prior results through the reducer,
and display only bounded structured outcome rows. Reuse native semantic helpers
and the existing diagnostic styles. Apply the exact copy, field order, 4px
spacing scale, typography, palette, 44px controls, focus outline, wrap-anywhere,
reduced-motion, 1023px single-column, and 320px overflow contract. Do not add a
launcher, role picker, presenter/no-scroll mode, Debug mode, ping tally,
cold-start/rehearsal scorer, evidence browser, animation, or component library.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npx playwright test apps/modem-ui/e2e/sound-proof.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - All nine UI-SPEC testable acceptance criteria pass in the production page.
    - Evidence class is always visible and Fixture/Loopback never uses Open-air success treatment.
    - Native disabled/busy controls and adjacent blocking copy prevent inaccessible hidden state.
    - Raw output, nsecs, packet bytes, raw control JSON, arbitrary errors, logs, and user-agent strings never enter the proof DOM.
    - The existing page can scroll; no Phase 5 viewport/presenter assertion is added.
  </acceptance_criteria>
  <done>The existing diagnostic UI exactly renders and controls the current Sound-only FIPS proof without overstating nonphysical evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local proof JSON → browser reducer/DOM | Structured runtime facts are untrusted until exact-schema validation. |
| Reducer state → privileged ping control | Rendering logic must not create authority absent from current proof state. |

## ASVS L1 Applicability and Controls

| Category | Applies | Task/test control |
|---|---|---|
| V2 Authentication | Yes | UI tests prove authenticated-peer display comes only from server proof facts, never bridge/audio inference. |
| V3 Session/Replay | Yes | Reducer tests clear outcomes on epoch/session/peer changes and require a fresh GET before another POST. |
| V4 Authorization/Topology | Yes | Role B never renders Run; Role A native control stays disabled unless current server readiness authorizes it. |
| V5 Input/Process Invocation | Yes | Strict proof parser and exact route/method/body client calls are asserted; browser never constructs command arguments. |
| V6 Identity/Evidence Integrity | Yes | Identity, evidence class, attestation disposition, and Open-air state are rendered from bounded server facts without client inference. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04-01 | Spoofing | proof card | high | mitigate | Render only strict Plan 04-03 facts and preserve source/evidence class; bridge/audio state cannot infer FIPS proof. |
| T-04-04-02 | Elevation of Privilege | ping button | high | mitigate | Native disabled control derives from current Role A Ready state; server rechecks all gates independently. |
| T-04-04-03 | Replay | prior outcome | high | mitigate | Reducer clears result on every epoch/readiness/peer/link/isolation change and needs a fresh snapshot before another ping. |
| T-04-04-04 | Information Disclosure | browser DOM | medium | mitigate | Render bounded safe scalars only and cap/wrap safe reasons; prohibit raw output, config, logs, and secrets. |
| T-04-04-05 | Denial of Service | refresh/ping UI | medium | mitigate | One in-flight fetch/action guard, bounded response parser, disabled duplicate actions, and visible recovery. |
| T-04-04-SC | Tampering | UI dependency surface | low | accept | Existing native Vite/TypeScript/CSS stack only; no registry package or component library. |
</threat_model>

<verification>
- Run proof reducer unit tests and the focused production Playwright spec.
- Run typecheck/build and existing diagnostic UI tests to detect regressions.
- Inspect a 320px screenshot and semantic accessibility snapshot in Playwright.
</verification>

<success_criteria>
- Operators can visibly show every DEMO-03/DEMO-04 fact before and during ping.
- Role/state/evidence controls exactly match 04-UI-SPEC.
- All 24 state considerations and nine acceptance criteria are executable and passing.
</success_criteria>

<output>
Create `.planning/phases/04-prove-the-sound-only-fips-ping/04-04-SUMMARY.md` when done.
</output>
