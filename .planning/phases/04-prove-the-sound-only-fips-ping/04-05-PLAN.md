---
phase: 04-prove-the-sound-only-fips-ping
plan: "05"
type: execute
wave: 5
depends_on: [04-04]
files_modified: []
autonomous: false
requirements: [FIPS-04, FIPS-05, DEPLOY-03, DEPLOY-04, DEPLOY-05, DEMO-01, DEMO-02, DEMO-03, DEMO-04, CONFIG-04]

must_haves:
  truths:
    - "Two named physical laptops establish a current authenticated encrypted FIPS Sound peer only after the Phase 3 acoustic readiness gate; otherwise the result remains `human_needed` per D-01, D-02, and D-13."
    - "Role B live snapshots show exactly one usable FIPS transport with current Sound state/worker/readiness and expected peer/link association, while local browser endpoints remain loopback-only per D-04 and D-05."
    - "One real system `ping -6` in Role A's live FIPS namespace returns one real ICMPv6 echo reply from B's configured `fips0` address across the Sound link per D-08 through D-10 and D-14."
    - "One physical interruption disarms first, clears the outcome, exposes degraded state, and returns through acoustic recovery plus normal FIPS re-authentication without restart/failover per D-03 and D-11."
    - "Both screens visibly correlate current peer/link/isolation, acoustic and packet/fragment/integrity/retry counters, and ICMPv6 result from observed facts per D-12."
  artifacts:
    - path: "Runtime Role A and Role B proof records"
      provides: "Matching named-machine Open-air facts or explicit human_needed disposition"
    - path: "Runtime Role A ping result"
      provides: "Raw bounded system ping output plus structured outcome/counter correlation"
  key_links:
    - from: "Role A kernel ping"
      to: "Role B fips0"
      via: "authenticated FIPS Sound peer carried by the physical acoustic session"
      pattern: "ping -6"
    - from: "physical interruption"
      to: "fresh authenticated peer"
      via: "ACOUSTIC_DISARM then normal acoustic/FIPS reconnect"
      pattern: "degraded|reconnecting|authenticated"
---

## Phase Goal

**As a** demo participant, **I want to** reach the otherwise isolated FIPS
node as an authenticated acoustic peer of the wider mesh, **so that** it
returns a real kernel IPv6 echo reply across the same sound link.

<objective>
Perform the irreducibly physical Phase 4 acceptance gate on the exact two-laptop open-air setup without fabricating results when hardware is absent.

Purpose: Confirm the implemented software produces the project’s core real-world outcome and one bounded normal reconnect.
Output: Matching Open-air records with one successful kernel ping and interruption/reconnect proof, or an honest `human_needed` result.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-CONTEXT.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-UI-SPEC.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-VALIDATION.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-01-SUMMARY.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-02-SUMMARY.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-03-SUMMARY.md
@.planning/phases/04-prove-the-sound-only-fips-ping/04-04-SUMMARY.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-VERIFICATION.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Accept one physical Sound-only ping and one normal reconnect</name>
  <what-built>Normal FIPS authentication over the Phase 3 acoustic gate, role-specific Sound-only isolation, live structured proof status, one bounded Role A kernel ping, and disarm-first normal reconnect.</what-built>
  <how-to-verify>
    1. On two named physical laptops, start the already-implemented Phase 4 role stacks using role A and role B configuration. Do not add a LAN browser/bridge connection between them.
    2. Arm both real browser audio paths and wait for matching settings digest, COMMIT_ACK, current heartbeat, then a current authenticated expected FIPS peer and joined Sound link.
    3. On Role B, refresh proof status and confirm live `show_transports` plus Compose inspection report exactly one usable transport, Sound. Confirm its browser endpoint is loopback-only.
    4. Refresh Role A proof status. Confirm it sends the one-use IPv6 UDP challenge from A `fips0` to B's fixed `fips0` proof port and accepts the nonce-bound current isolation attestation only over the authenticated FIPS/Sound path. Do not transfer a file or use a browser/LAN path.
    5. Press `Run sound-only ping` once. Confirm the structured result identifies Role A FIPS namespace/system `ping -6`, B's configured `fips0` target, exit 0, one sequence/latency/loss summary, and observed before/after packet/acoustic/fragment/integrity/retry counters.
    6. Confirm both named records qualify as matching `Open air`. If either record is absent, mismatched, Fixture, or Loopback, record `human_needed`; do not approve the physical claim.
    7. Interrupt the browser/acoustic path long enough to disarm/degrade. Confirm the old ping result clears before peer loss, no FIPS daemon restarts, and no other transport takes over.
    8. Restore the acoustic path, allow the bounded existing recovery/recalibration path, wait for a fresh current heartbeat and normal FIPS re-authentication, then confirm proof status becomes Ready again. One successful ping before interruption is sufficient for Phase 4; do not run Phase 5's ten-ping/cold-start/rehearsal sequence.
  </how-to-verify>
  <resume-signal>Type "approved" only if the exact two-laptop Open-air ping and reconnect observations pass; otherwise report `human_needed` with the missing physical gate or describe the failure.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Physical laptops/room → Open-air evidence | Fixture or one-machine observations cannot prove the intended hop. |
| Operator observation → phase acceptance | Acceptance must preserve exact machine, runtime, command, isolation, and evidence-class facts. |

## ASVS L1 Applicability and Controls

| Category | Applies | Checkpoint control |
|---|---|---|
| V2 Authentication | Yes | Human confirms the expected normally authenticated FIPS Sound peer before attestation/ping. |
| V3 Session/Replay | Yes | Human observes current epoch, one-use challenge disposition, disarm, and fresh post-recovery authentication. |
| V4 Authorization/Topology | Yes | Human confirms Role B exact one usable Sound transport and no host-published proof/browser bypass. |
| V5 Input/Process Invocation | Yes | Automated pre-gate proves exact routes/datagram bounds and one fixed system ping before physical acceptance. |
| V6 Identity/Evidence Integrity | Yes | Matching named-machine identity, in-band attestation bindings, raw ping evidence, and Open-air disposition are retained. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-05-01 | Spoofing | physical peer/path | high | mitigate | Require matching named-machine Open-air records plus authenticated expected Sound peer/link facts. |
| T-04-05-02 | Elevation of Privilege | Role B isolation | high | mitigate | Verify live transport cardinality and Compose boundary before the ping. |
| T-04-05-03 | Repudiation | kernel ping result | high | mitigate | Retain raw bounded system output, exit, target, timestamps, and structured counter correlation. |
| T-04-05-04 | Replay | post-interruption pass | high | mitigate | Require disarm/clear then fresh current acoustic heartbeat and FIPS authentication before Ready returns. |
| T-04-05-05 | Denial of Service | room/hardware unavailability | low | accept | Return `human_needed`; never manufacture or promote deterministic evidence. |
| T-04-05-SC | Tampering | dependencies | low | accept | No install or source mutation occurs in this verification-only plan. |
</threat_model>

<verification>
- Before the checkpoint, run all automated commands in `04-VALIDATION.md`.
- Physical speaker/microphone, exact two-laptop Open-air behavior, final kernel reply, and real loss/recovery remain human-only.
</verification>

<success_criteria>
- Exact two-laptop Open-air evidence proves one real request/reply and one bounded normal reconnect.
- When target hardware is absent or any record is nonphysical/mismatched, phase status remains `human_needed`.
</success_criteria>

<output>
Create `.planning/phases/04-prove-the-sound-only-fips-ping/04-05-SUMMARY.md` when done.
</output>
