---
phase: 03-negotiate-and-deliver-reliable-acoustic-packets
plan: "04"
type: execute
wave: 2
depends_on: [03-03]
files_modified:
  - apps/modem-ui/src/acoustic-session.ts
  - apps/modem-ui/src/acoustic-session.test.ts
autonomous: true
requirements: [LINK-02, NEG-01, NEG-02, NEG-03, NEG-04, NEG-05]

must_haves:
  truths:
    - "Only role A initiates discovery and owns the first transmit turn; role B answers for the configured complementary peer."
    - "EDGE[NEG-01/unclassified]: the conservative exact profile is used for discovery, control, recovery, and recalibration even when directional data settings later differ."
    - "EDGE[NEG-02/adjacency]: capability values exactly on an advertised mutual boundary are accepted and one step outside either peer's range rejects."
    - "EDGE[NEG-02/empty]: missing identity, expected peer, role, nonce, profile set, or mandatory range rejects before state transition."
    - "EDGE[NEG-02/ordering]: duplicate/out-of-order handshake units are idempotently ignored or rejected and cannot skip the legal state sequence."
    - "EDGE[NEG-03/unclassified]: four numbered A→B probes and then four numbered B→A probes preserve valid, corrupt, missing, duplicate, discontinuity, timing, signal, clipping, and confidence observations."
    - "EDGE[NEG-04/unclassified]: deterministic selection is per direction, prioritizes byte correctness then timing/lower gain, and uses bootstrap fallback only when its hard gates pass."
    - "EDGE[NEG-05/unclassified]: readiness remains false until both roles hold the identical settings digest, receive COMMIT_ACK, and then observe a current-session heartbeat."
  artifacts:
    - path: "apps/modem-ui/src/acoustic-session.ts"
      provides: "Nonce-bound A/B handshake, literal bidirectional calibration, selection, settings commitment, heartbeat readiness, and reset"
      exports: ["AcousticSession", "AcousticSessionSnapshot", "AcousticModem", "AcousticClock", "AcousticTimers"]
    - path: "apps/modem-ui/src/acoustic-session.test.ts"
      provides: "Deterministic fake-modem/fake-clock negotiation and calibration proof"
  key_links:
    - from: "apps/modem-ui/src/acoustic-session.ts"
      to: "apps/modem-ui/src/acoustic-protocol.ts"
      via: "strict FAS1 decode and canonical settings digest"
      pattern: "decodeFas1|digestSettings"
    - from: "apps/modem-ui/src/acoustic-session.ts"
      to: "packages/bridge/src/demo-config.ts"
      via: "injected validated candidates and bounded deadlines"
      pattern: "calibration"
---

## Phase Goal

**As a** demo operator, **I want to** establish a measured and mutually
committed acoustic session between both laptops, **so that** complete FIPS
packets receive safe bounded deterministic half-duplex delivery below the FIPS
boundary.

<objective>
Establish a nonce-bound A/B acoustic session, measure both literal directions, select directional settings, and mutually commit one digest before readiness.

Purpose: Convert local audio capability into a peer-measured, deterministic, fail-closed session suitable for the reliable packet scheduler.
Output: A pure injected session state machine and deterministic two-role fake-modem/fake-clock test suite.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-CONTEXT.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-RESEARCH.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-PATTERNS.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-VALIDATION.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-03-SUMMARY.md
</context>

## Artifacts this phase produces

- `AcousticSession` with explicit role/state/epoch/session authority.
- Injected `AcousticModem`, monotonic clock, and timer seams plus deterministic
  two-role fixture.
- Capability intersection, literal A→B/B→A calibration ledger, deterministic
  directional selection, COMMIT/COMMIT_ACK, heartbeat readiness, and atomic
  reset behavior.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Complete the nonce-bound A-initiated bootstrap handshake</name>
  <read_first>
    - `03-RESEARCH.md` Pattern 2 state machine and Security Domain.
    - `03-PATTERNS.md` acoustic-session analog and epoch/generation rules.
    - `apps/modem-ui/src/cyrinx-case-watchdog.ts` injected timer/idempotent callback pattern.
    - `apps/modem-ui/src/qualification-session.ts` numbered observation/deduplication pattern.
    - `03-03-SUMMARY.md` exact FAS1/profile/settings APIs and bounds.
  </read_first>
  <files>apps/modem-ui/src/acoustic-session.test.ts, apps/modem-ui/src/acoustic-session.ts</files>
  <behavior>
    - Role A alone emits HELLO and owns the first turn; B in Listening answers.
    - Version, configured public identity, expected peer, complementary role, both 128-bit nonces, epoch, profile set, and capability ranges bind the session.
    - Stale/wrong/corrupt/unsupported/out-of-state/duplicate units cannot advance state.
    - Reset cancels timers and atomically clears session ID, nonce, turns, settings, heartbeat, and readiness.
  </behavior>
  <action>
Build the fake modem/clock/timer tests first, then implement a pure
`AcousticSession` with explicit states and legal transitions. Generate nonces
through an injected source backed by `crypto.getRandomValues` in production.
Validate FAS1 before checking current epoch, role, configured identities,
nonces, sequence, capabilities, and transition. Make role A the sole initiator
and first owner. Implement reset as one synchronous invalidation before any new
snapshot is exposed.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run apps/modem-ui/src/acoustic-session.test.ts -t "bootstrap|handshake|reset"</automated>
  </verify>
  <acceptance_criteria>
    - Two fixture roles reach the first calibration state through real encoded FAS1 units.
    - Every binding field has a negative test.
    - Late callbacks and duplicate/out-of-order units cannot revive or skip state.
  </acceptance_criteria>
  <done>The configured A/B pair establishes one bounded nonce-bound session and rejects ambient/stale traffic.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Measure both directions and commit deterministic settings</name>
  <read_first>
    - `03-CONTEXT.md` Calibration and settings commitment decisions.
    - `03-RESEARCH.md` Pattern 3 measured directional calibration and COMMIT semantics.
    - `03-VALIDATION.md` NEG-03..05 validation rows.
    - `apps/modem-ui/src/qualification-session.ts` result ledger semantics.
    - The session state machine and fixture from Task 1.
  </read_first>
  <files>apps/modem-ui/src/acoustic-session.ts, apps/modem-ui/src/acoustic-session.test.ts</files>
  <behavior>
    - The warm-start candidate is tried first, then at most three candidates/four probes under one 120-second deadline.
    - A→B completes and reports before B→A begins; every observation category is bounded and copied.
    - Selection may differ by direction and follows hard byte-correctness gates, timing, lower gain, then payload.
    - Both roles canonicalize identical directional settings; mismatch/old COMMIT_ACK rejects.
    - COMMIT_ACK alone is insufficient; a subsequent current heartbeat makes the session ready.
  </behavior>
  <action>
Extend the deterministic fixture with numbered PROBE/REPORT exchange in literal
A→B then B→A order. Preserve only genuinely measurable bounded observations
and calculate deterministic directional choices from the configured
candidates, including the non-clipping gain candidate up to 2.0 and safe
bootstrap fallback. Exchange canonical COMMIT and COMMIT_ACK digest bytes.
Expose ready only after matching acknowledgement plus a current heartbeat.
No local playback callback, loudness, or nominal throughput may substitute for
remote measurement.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run apps/modem-ui/src/acoustic-session.test.ts -t "calibration|selection|commit|heartbeat"</automated>
  </verify>
  <acceptance_criteria>
    - Both literal directions have complete bounded ledgers.
    - Directional asymmetry and deterministic tie-breaking are tested.
    - Deadline/fallback/mismatch paths end safely and visibly.
    - Readiness requires matching commit acknowledgement and a later current heartbeat.
  </acceptance_criteria>
  <done>Both roles hold measured directional settings and one mutually acknowledged digest before session readiness.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Ambient acoustic unit → active session | Unauthenticated room traffic could spoof state transitions. |
| Peer calibration report → settings selection | Corrupt or stale measurements could select an unsafe profile. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-04-01 | Spoofing | bootstrap handshake | high | mitigate | Bind expected identities, complementary roles, both nonces, session ID, epoch, and legal state. |
| T-03-04-02 | Tampering | calibration reports | high | mitigate | Accept strict current-session numbered reports and retain per-outcome evidence before deterministic selection. |
| T-03-04-03 | Replay | COMMIT_ACK/heartbeat | high | mitigate | Require current session/digest/sequence and a heartbeat after acknowledgement. |
| T-03-04-04 | Denial of Service | calibration | high | mitigate | Bound candidates, probes, timers, observations, and total deadline; exhaust to a safe error. |
| T-03-04-05 | Repudiation | settings agreement | medium | mitigate | Both roles expose the same canonical digest and explicit ack/heartbeat facts. |
| T-03-04-SC | Tampering | dependency surface | low | accept | Pure TypeScript and browser primitives only; no install occurs. |
</threat_model>

<verification>
- Run focused bootstrap and calibration Vitest selections under Node 22.
- Run the full `acoustic-session.test.ts`.
- Confirm all seven named edge truths have explicit passing cases.
</verification>

<success_criteria>
- A and B establish one valid session.
- Literal bidirectional measurement selects deterministic directional settings.
- Readiness remains false until commit acknowledgement and current heartbeat.
</success_criteria>

<output>
Create `.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-04-SUMMARY.md` when done.
</output>
