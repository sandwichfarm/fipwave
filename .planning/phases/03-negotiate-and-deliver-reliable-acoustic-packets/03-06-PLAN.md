---
phase: 03-negotiate-and-deliver-reliable-acoustic-packets
plan: "06"
type: execute
wave: 3
depends_on: [03-02, 03-04]
files_modified:
  - packages/bridge/src/protocol.ts
  - packages/bridge/src/server.ts
  - packages/bridge/test/fips-packet-bridge.test.ts
  - vendor/fips/src/transport/sound/mod.rs
autonomous: true
requirements: [LINK-02, LINK-09, NEG-05, NEG-06]

must_haves:
  truths:
    - "AUDIO_SETTINGS records local audio preflight only and never sends BROWSER_ARM or permits a FIPS packet."
    - "Only a current-epoch acoustic-ready projection emitted after commit acknowledgement plus current heartbeat arms the Rust packet gate."
    - "Reset, browser disconnect, Degraded, terminal Error, stale epoch, and session expiry disarm before clearing queued work."
    - "Rust worker Up remains distinct from acoustic readiness and rejects packet send/injection while disarmed."
    - "Bridge and Rust status expose bounded scalar readiness/counters/error facts without packet, nonce, nsec, or raw digest content."
  artifacts:
    - path: "packages/bridge/src/protocol.ts"
      provides: "Current-epoch ACOUSTIC_READY and ACOUSTIC_DISARM local controls"
    - path: "packages/bridge/src/server.ts"
      provides: "Session-derived arm/disarm projection and truthful bridge snapshot"
    - path: "packages/bridge/test/fips-packet-bridge.test.ts"
      provides: "AUDIO_SETTINGS/readiness/reset/disconnect/degraded integration tests"
    - path: "vendor/fips/src/transport/sound/mod.rs"
      provides: "Current-epoch fail-closed acoustic readiness packet gate and Rust tests"
  key_links:
    - from: "packages/bridge/src/server.ts"
      to: "vendor/fips/src/transport/sound/mod.rs"
      via: "ACOUSTIC_READY/DISARM translates to current-epoch BROWSER_ARM/DISARM"
      pattern: "ACOUSTIC_READY|ACOUSTIC_DISARM"
    - from: "apps/modem-ui/src/acoustic-session.ts"
      to: "packages/bridge/src/server.ts"
      via: "only committed+heartbeat readiness may originate acoustic-ready"
      pattern: "acoustic"
---

## Phase Goal

**As a** demo operator, **I want to** establish a measured and mutually
committed acoustic session between both laptops, **so that** complete FIPS
packets receive safe bounded deterministic half-duplex delivery below the FIPS
boundary.

<objective>
Replace local-audio arming with a current-epoch projection of mutually committed, heartbeat-current acoustic readiness across the bridge and Rust transport.

Purpose: Prevent FIPS from accepting packets while the browser has only microphone/speaker preflight or while the acoustic peer is degraded/reset.
Output: Strict readiness controls, bridge translation/snapshot behavior, Rust packet gating, and cross-language focused tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-CONTEXT.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-RESEARCH.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-PATTERNS.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-VALIDATION.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-02-SUMMARY.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-04-SUMMARY.md
</context>

## Artifacts this phase produces

- Strict current-epoch `ACOUSTIC_READY` / `ACOUSTIC_DISARM` FWAV controls.
- Bridge readiness translation that leaves `AUDIO_SETTINGS` local-only.
- Rust SoundTransport readiness semantics and reset/degraded/error gating tests.
- Secret-safe scalar readiness/counter projection.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make the bridge project acoustic session readiness only</name>
  <read_first>
    - `03-RESEARCH.md` Critical integration correction and Pitfall 1.
    - `03-PATTERNS.md` protocol/server analog and exact current arm behavior.
    - `packages/bridge/src/protocol.ts` control validation rules.
    - `packages/bridge/src/server.ts` AUDIO_SETTINGS and `notifyFipsBrowserState`.
    - `packages/bridge/test/fips-packet-bridge.test.ts` current endpoint/reset fixture.
  </read_first>
  <files>packages/bridge/src/protocol.ts, packages/bridge/src/server.ts, packages/bridge/test/fips-packet-bridge.test.ts</files>
  <behavior>
    - AUDIO_SETTINGS updates local preflight state and emits no BROWSER_ARM.
    - Current-epoch ACOUSTIC_READY with zero payload/sequence arms once; stale/malformed controls reject.
    - ACOUSTIC_DISARM, reset, browser close, Degraded, and Error send one disarm before queue clearing.
    - Repeated arm/disarm is idempotent and snapshots remain secret-safe/bounded.
  </behavior>
  <action>
Write bridge integration cases first. Add exact zero-payload current-epoch
acoustic-ready/disarm controls and separate local audio-preflight state from
session readiness. Translate only acoustic-ready into the existing FIPS-side
arm control. On every reset/disconnect/degraded/error path, disarm first and
then clear packet/session state. Preserve epoch/role/sequence validation and
bounded scalar snapshots.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run packages/bridge/test/fips-packet-bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - AUDIO_SETTINGS cannot arm the FIPS endpoint.
    - Only exact current acoustic-ready does.
    - Every invalidation path disarms once before cleanup.
    - Status distinguishes local audio, acoustic session, and FIPS endpoint facts.
  </acceptance_criteria>
  <done>The bridge admits FIPS traffic only from a current session-derived readiness projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce current acoustic readiness inside SoundTransport</name>
  <read_first>
    - `vendor/fips/src/transport/sound/mod.rs` browser-ready gate, inbound control validation, reset, send, and tests.
    - `03-01-SUMMARY.md` TrafficClass FWAV changes that must remain intact.
    - The exact controls and translation implemented by Task 1.
    - `03-VALIDATION.md` Rust readiness Wave 0 requirements.
  </read_first>
  <files>vendor/fips/src/transport/sound/mod.rs</files>
  <behavior>
    - Worker Up plus local audio settings cannot accept outgoing or incoming FIPS_PACKET bytes.
    - Current-epoch translated arm permits packet send/injection; stale/wrong-payload/wrong-sequence arm rejects.
    - Reset/disarm clears readiness before queues and old-generation callbacks cannot restore it.
    - Control/state output distinguishes worker, acoustic readiness, congestion, and safe last error.
  </behavior>
  <action>
Add focused Rust tests before changing semantics. Keep the codec-neutral
`browser_ready` storage if it remains the narrowest internal field, but make its
only producer the bridge's validated current acoustic-ready translation.
Retain zero-payload/zero-sequence/exact-epoch checks, class metadata, queue
rollback, and complete opaque packets. Clear readiness synchronously before
reset/disarm cleanup and reject all stale work.
  </action>
  <verify>
    <automated>cd vendor/fips &amp;&amp; cargo test sound_transport_readiness --locked &amp;&amp; cargo test sound_transport_traffic_class --locked</automated>
  </verify>
  <acceptance_criteria>
    - Rust focused tests prove preflight-only send/injection rejection.
    - Current ready permits and every invalidation disarms.
    - TrafficClass and complete-packet semantics from 03-01 remain green.
  </acceptance_criteria>
  <done>SoundTransport is fail-closed on the committed current acoustic session rather than local browser audio.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser session projection → bridge | A false/stale readiness claim could arm FIPS. |
| Bridge control → Rust packet gate | Control validation determines whether packets enter the trusted transport. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-06-01 | Elevation of Privilege | readiness projection | high | mitigate | AUDIO_SETTINGS cannot arm; only exact current acoustic-ready controls reach the Rust gate. |
| T-03-06-02 | Replay | stale arm/control | high | mitigate | Validate epoch, endpoint role, zero payload/sequence, and generation before mutation. |
| T-03-06-03 | Denial of Service | reset/degraded queues | high | mitigate | Disarm synchronously before bounded queue cleanup and reject late callbacks. |
| T-03-06-04 | Information Disclosure | readiness snapshot | medium | mitigate | Project allowlisted scalars and safe codes only. |
| T-03-06-SC | Tampering | dependency surface | low | accept | No dependency or external service changes. |
</threat_model>

<verification>
- Run bridge readiness Vitest under Node 22.
- Run both focused Rust sound tests.
- Run Node typecheck and `cargo fmt --check`.
</verification>

<success_criteria>
- Local audio preflight never authorizes packets.
- Commit-ack plus current-heartbeat projection is the only arm path.
- Reset/degraded/error disarms bridge and Rust atomically.
</success_criteria>

<output>
Create `.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-06-SUMMARY.md` when done.
</output>
