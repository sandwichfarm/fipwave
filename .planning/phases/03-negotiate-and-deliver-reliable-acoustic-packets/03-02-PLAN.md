---
phase: 03-negotiate-and-deliver-reliable-acoustic-packets
plan: "02"
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - packages/bridge/src/protocol.ts
  - packages/bridge/src/server.ts
  - packages/bridge/test/fips-packet-bridge.test.ts
  - apps/modem-ui/src/fips-packet-adapter.ts
  - apps/modem-ui/src/fips-packet-adapter.test.ts
autonomous: true
requirements: [LINK-08]

must_haves:
  truths:
    - "The Rust-authored TrafficClass survives strict FWAV decoding, bridge forwarding, and browser FipsPacketAdapter delivery as metadata beside byte-identical packet bytes."
    - "The bridge validates class and epoch before queue mutation and preserves bounded independent packet queues."
    - "The browser adapter exposes class to its callback without parsing or changing the complete packet."
    - "Browser scheduling receives a trustworthy class value for ACK/control, FIPS liveness, and ordinary data ordering."
  artifacts:
    - path: "packages/bridge/src/protocol.ts"
      provides: "Validated FWAV FipsTrafficClass metadata"
      exports: ["FipsTrafficClass"]
    - path: "packages/bridge/src/server.ts"
      provides: "Class-preserving bounded local relay"
    - path: "packages/bridge/test/fips-packet-bridge.test.ts"
      provides: "Rust-compatible class propagation integration coverage"
    - path: "apps/modem-ui/src/fips-packet-adapter.ts"
      provides: "Complete packet plus TrafficClass browser boundary"
      exports: ["FipsPacketEnvelope", "FipsPacketAdapter"]
  key_links:
    - from: "vendor/fips/src/transport/sound/mod.rs"
      to: "packages/bridge/src/protocol.ts"
      via: "Identical FWAV class discriminant contract from 03-01"
      pattern: "TrafficClass"
    - from: "packages/bridge/src/server.ts"
      to: "apps/modem-ui/src/fips-packet-adapter.ts"
      via: "binary FIPS_PACKET forwarding with preserved class"
      pattern: "trafficClass"
  prohibitions:
    - statement: "MUST NOT create or use any bridge, WebSocket, fixture, or browser networking path between the two laptops; the only inter-laptop path remains audible sound."
      category: "transparency"
      status: "unresolved"
      flagged: true
---

## Phase Goal

**As a** demo operator, **I want to** establish a measured and mutually
committed acoustic session between both laptops, **so that** complete FIPS
packets receive safe bounded deterministic half-duplex delivery below the FIPS
boundary.

<objective>
Carry the explicit FIPS traffic class through the existing local codec-neutral bridge into the browser packet adapter.

Purpose: Finish the LINK-08 source trace at the acoustic scheduler boundary without weakening the local-only topology or opaque packet contract.
Output: Strict TypeScript FWAV class metadata, class-preserving bridge queues, and a browser adapter envelope with adversarial tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-CONTEXT.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-RESEARCH.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-PATTERNS.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-VALIDATION.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-01-SUMMARY.md
</context>

## Artifacts this phase produces

- `FipsTrafficClass` and strict FWAV class encode/decode behavior.
- Class-preserving browser/FIPS bridge queues and integration tests.
- `FipsPacketEnvelope` at the browser boundary.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Preserve TrafficClass through FWAV and the bounded bridge</name>
  <read_first>
    - `03-01-SUMMARY.md` for exact Rust class discriminants and wire placement.
    - `03-PATTERNS.md` bridge protocol/server analog assignments.
    - `packages/bridge/src/protocol.ts` complete FWAV decoder validation order.
    - `packages/bridge/src/server.ts` FIPS packet queue admission and forwarding.
    - `packages/bridge/test/fips-packet-bridge.test.ts` two-endpoint integration fixture.
  </read_first>
  <files>packages/bridge/src/protocol.ts, packages/bridge/src/server.ts, packages/bridge/test/fips-packet-bridge.test.ts</files>
  <behavior>
    - A Control, Heartbeat, or Ordinary FIPS_PACKET round-trips through encode/decode and bridge forwarding with unchanged bytes.
    - Unknown class, wrong message type, stale epoch, invalid sequence, and oversize payload reject before queue/counter mutation.
    - Class preservation does not create another endpoint or listener.
  </behavior>
  <action>
Start with failing protocol and two-endpoint bridge cases. Add the exact
Rust-compatible class discriminant to the existing fixed FWAV metadata, validate
it before dispatch, and carry it through the bounded packet item rather than a
parallel side channel. Preserve current epoch, role, queue-item, byte, and age
checks. Keep both endpoints loopback-local and do not add any inter-browser or
inter-laptop connection.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run packages/bridge/test/fips-packet-bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All three exact classes survive a real local bridge round trip.
    - Invalid class/epoch/sequence/size cannot advance accepted counters.
    - Payload bytes and loopback-only topology remain unchanged.
  </acceptance_criteria>
  <done>The bounded local bridge safely transports source-authored FIPS priority metadata.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver class and complete bytes through FipsPacketAdapter</name>
  <read_first>
    - `apps/modem-ui/src/fips-packet-adapter.ts` complete-packet arm/generation contract.
    - `apps/modem-ui/src/fips-packet-adapter.test.ts` current adapter test style.
    - The class-preserving TypeScript frame contract created by Task 1.
    - `03-CONTEXT.md` framing/reliability and health decisions.
  </read_first>
  <files>apps/modem-ui/src/fips-packet-adapter.ts, apps/modem-ui/src/fips-packet-adapter.test.ts</files>
  <behavior>
    - Current armed packets invoke the callback once with copied complete bytes and the exact class.
    - Stale epoch/generation and unarmed packets reject without callback.
    - Mutating caller-owned input after send cannot alter delivered bytes or class.
  </behavior>
  <action>
Evolve the adapter's complete-packet callback to receive a typed envelope
containing copied bytes and the validated traffic class. Preserve its tiny
codec-neutral scope: it must know neither FAS1 fragments nor Quiet profiles.
Keep epoch/generation admission fail-closed and add tests for every class,
copying, stale lifecycle, and invalid metadata.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run apps/modem-ui/src/fips-packet-adapter.test.ts packages/bridge/test/fips-packet-bridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Browser callbacks receive only validated class plus complete copied packet bytes.
    - The adapter retains no payload parser or codec knowledge.
    - Lifecycle failures are explicit and cannot queue work.
  </acceptance_criteria>
  <done>The LINK-08 priority signal reaches the browser scheduler boundary without inspecting FIPS bytes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Rust FWAV → Node bridge | Binary metadata and payload cross a parser boundary. |
| Node bridge → browser adapter | Local WebSocket data crosses into browser scheduling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-02-01 | Tampering | FWAV class field | high | mitigate | Exact-enum validation precedes queue mutation and invalid values are adversarially tested. |
| T-03-02-02 | Elevation of Privilege | browser scheduling metadata | high | mitigate | Only source-authored validated values cross; no payload-derived promotion exists. |
| T-03-02-03 | Spoofing | alternate laptop path | high | mitigate | Preserve the existing loopback-only endpoints and assert no new listener/peer connection in integration tests. |
| T-03-02-04 | Denial of Service | bridge queues | high | mitigate | Retain packet item/byte/age limits while adding one bounded scalar field. |
| T-03-02-SC | Tampering | dependency surface | low | accept | No new package, service, or external API is introduced. |
</threat_model>

<verification>
- Run both focused Vitest files under the required Node 22 PATH.
- Run `npm run typecheck` under Node 22.
- Inspect the diff for zero payload parsers and zero new listening/network endpoints.
</verification>

<success_criteria>
- TrafficClass survives Rust-to-browser propagation.
- Invalid metadata rejects before state mutation.
- The local bridge remains codec-neutral and laptop-local.
</success_criteria>

<output>
Create `.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-02-SUMMARY.md` when done.
</output>
