---
phase: 03-negotiate-and-deliver-reliable-acoustic-packets
plan: "05"
type: execute
wave: 3
depends_on: [03-02, 03-04]
files_modified:
  - apps/modem-ui/src/acoustic-session.ts
  - apps/modem-ui/src/acoustic-session.test.ts
autonomous: true
requirements: [LINK-03, LINK-04, LINK-05, LINK-06, LINK-07, LINK-08, LINK-09, NEG-06]

must_haves:
  truths:
    - "A complete opaque 1357-byte packet crosses the deterministic two-role fixture in each direction and is delivered exactly once after bounded FAS1 fragmentation/reassembly."
    - "EDGE[LINK-05/unclassified]: duplicate DATA, TURN_END, ACK, retry, and delivered-packet replay produce at most one local complete-packet delivery for a session plus packet ID."
    - "EDGE[LINK-06/idempotency]: repeating the same timeout or ACK transition cannot consume an extra retry, deliver twice, or advance two turns."
    - "EDGE[LINK-06/concurrency]: simultaneous timer/unit events are serialized by current session/turn generation; reset or ACK wins once and stale work is ignored."
    - "EDGE[LINK-07/adjacency]: a response scheduled exactly at local completion plus guard is valid; any earlier attempted opposite-role transmission is rejected as overlap."
    - "EDGE[LINK-07/empty]: an owner with no packet data uses explicit bounded heartbeat/turn behavior and does not emit an empty DATA burst or deadlock silently."
    - "EDGE[LINK-07/ordering]: control and ACK are stable first, then FIPS Control/Heartbeat, then Ordinary; equal-class packets retain FIFO order."
    - "EDGE[LINK-09/unclassified]: item/byte/packet/age cap exhaustion rejects with a stable visible code, disarms readiness, and retains no unbounded packet work."
    - "EDGE[NEG-06/idempotency]: repeated missed-heartbeat signals enter Degraded once and consume one bounded recovery path."
    - "EDGE[NEG-06/concurrency]: reset or a current heartbeat racing recovery deterministically cancels stale timers; exhaustion reaches one terminal actionable error."
  artifacts:
    - path: "apps/modem-ui/src/acoustic-session.ts"
      provides: "Four-unit burst ARQ, ACK bitmap, measured timeout/backoff, bounded reassembly/dedupe, priority queues, heartbeat degradation, and recovery"
      exports: ["AcousticDeliveryResult", "AcousticQueueResult"]
    - path: "apps/modem-ui/src/acoustic-session.test.ts"
      provides: "Deterministic loss, retry, collision, duplicate, expiry, backpressure, and recovery proof"
  key_links:
    - from: "apps/modem-ui/src/fips-packet-adapter.ts"
      to: "apps/modem-ui/src/acoustic-session.ts"
      via: "complete packet plus source-authored FIPS TrafficClass"
      pattern: "trafficClass"
    - from: "apps/modem-ui/src/acoustic-session.ts"
      to: "apps/modem-ui/src/acoustic-protocol.ts"
      via: "FAS1 fragmentation, strict decode, and bounded reassembly"
      pattern: "fragmentPacket|decodeFas1"
---

## Phase Goal

**As a** demo operator, **I want to** establish a measured and mutually
committed acoustic session between both laptops, **so that** complete FIPS
packets receive safe bounded deterministic half-duplex delivery below the FIPS
boundary.

<objective>
Deliver complete opaque packets with deterministic half-duplex burst/ACK reliability, bounded resource use, source-authored priority, and finite degradation/recovery.

Purpose: Turn the committed acoustic session into a safe bidirectional packet service without exposing fragments to FIPS or allowing loss to grow work indefinitely.
Output: Production ARQ/reassembly/priority/health behavior and exhaustive fake-clock/fake-modem tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-CONTEXT.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-RESEARCH.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-PATTERNS.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-VALIDATION.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-02-SUMMARY.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-04-SUMMARY.md
</context>

## Artifacts this phase produces

- Four-DATA-unit maximum burst, TURN_END, bitmap ACK, next-turn grant, and
  measured timeout/backoff behavior.
- Bounded one-direction reassembly, exactly-once delivered-ID history, expiry,
  packet queues, and stable safe errors.
- Source-authored class scheduler and finite heartbeat
  Ready→Degraded→Recovering→Ready/Error path.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Round-trip 1357-byte packets exactly once under loss</name>
  <read_first>
    - `03-RESEARCH.md` Patterns 4–5, Pitfalls 3–4, and 1357-byte geometry.
    - `03-PATTERNS.md` required scheduler ordering and bounded accounting.
    - `03-04-SUMMARY.md` session state, settings, fake modem, and timer APIs.
    - `apps/modem-ui/src/fips-packet-adapter.ts` complete packet/class envelope from 03-02.
    - `03-VALIDATION.md` LINK-03..07 rows.
  </read_first>
  <files>apps/modem-ui/src/acoustic-session.test.ts, apps/modem-ui/src/acoustic-session.ts</files>
  <behavior>
    - 1357 bytes travel A→B and B→A with seven 217-byte-profile fragments and exactly one callback per packet.
    - One lost DATA bit retries only that fragment; lost TURN_END or ACK retries the same window no more than three total attempts.
    - Duplicate units and a lost ACK after complete delivery replay ACK state but never redeliver.
    - Incomplete/expired/oversize/17-fragment/conflicting-declared-length assemblies never emit.
  </behavior>
  <action>
Add the complete-packet, loss, duplicate, and expiry cases before production
logic. Extend the active session with one inbound reassembly per direction,
copied fragments, a maximum four-unit burst, explicit TURN_END, bitmap ACK, and
next-turn grant. Derive timeout from measured p95 airtime/round trip plus guard
within 4–15 seconds, cap attempts at three, and retain 32 delivered packet IDs
for the configured bounded TTL. Treat local modem completion as a collision
boundary only; only remote ACK changes delivery/turn state.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run apps/modem-ui/src/acoustic-session.test.ts -t "packet|fragment|reassembly|retry|duplicate|turn"</automated>
  </verify>
  <acceptance_criteria>
    - Both roles deliver one byte-identical 1357-byte packet.
    - Retry transmits only missing fragments and stops after three attempts.
    - Duplicate/lost-ACK paths never create a second delivery.
    - Partial, expired, and out-of-bounds assemblies never cross the callback.
  </acceptance_criteria>
  <done>The committed session provides bounded exactly-once complete-packet delivery in both directions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Bound priority, backpressure, heartbeat, and recovery</name>
  <read_first>
    - `03-CONTEXT.md` Framing/reliability and Health/recovery decisions.
    - `03-RESEARCH.md` Pattern 4 priority/timeout formula and Assumption A2.
    - `03-02-SUMMARY.md` browser-visible FIPS TrafficClass contract.
    - `packages/bridge/src/demo-config.ts` validated queue/heartbeat/recovery bounds from 03-03.
    - The ARQ behavior from Task 1.
  </read_first>
  <files>apps/modem-ui/src/acoustic-session.ts, apps/modem-ui/src/acoustic-session.test.ts</files>
  <behavior>
    - ACK/session control preempts FIPS Control and Heartbeat; both preempt Ordinary while preserving same-class FIFO.
    - Four queued complete packets, one reassembly, 1357 bytes, 16 fragments, 32 delivered IDs, and configured age/expiry are hard caps.
    - Miss thresholds enter Degraded once, immediately make readiness false, and perform only configured retry/rebootstrap/recalibration attempts.
    - Recovery success requires a new valid commit/heartbeat; exhaustion emits one stable terminal code and leaves no armed timers/queues.
  </behavior>
  <action>
Implement separate bounded priority queues with the exact stable order and
explicit idle heartbeat/turn handoff. Reject overflow before retaining packet
bytes and expose safe queue/retry/drop/error counters. Add the configured
heartbeat deadline and finite Degraded/Recovering transitions; cancel every
generation-sensitive timer and pending send on reset or recovery replacement.
No loop may retry without decrementing a bound, and no local `onFinish` may
increment remote delivery.
  </action>
  <verify>
    <automated>PATH="/Users/sandwich/.npm/_npx/d295cebdb7c54afe/node_modules/node/bin:$PATH" ./node_modules/.bin/vitest run apps/modem-ui/src/acoustic-session.test.ts -t "priority|backpressure|heartbeat|degraded|recovery|concurrency"</automated>
  </verify>
  <acceptance_criteria>
    - Stable priority/FIFO order is under deterministic test.
    - Every item/byte/history/age/retry limit has exact/one-over coverage.
    - Degraded disarms immediately and recovery is finite.
    - Reset/race tests prove stale callbacks cannot rearm or redeliver.
  </acceptance_criteria>
  <done>Loss and slow delivery remain bounded, visible, deterministic, and recoverable without an infinite loop.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated FAS1 unit → reassembly/delivery | Valid framing can still replay, conflict, or exhaust state. |
| Timers/modem callbacks → active turn | Concurrent late effects could overlap transmissions or duplicate delivery. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-05-01 | Replay | delivered packet path | high | mitigate | Session-scoped packet IDs, bounded delivered history, idempotent ACK replay, and exactly-once callback tests. |
| T-03-05-02 | Denial of Service | queues/reassembly/retries | high | mitigate | Enforce item/byte/fragment/history/age/attempt caps before retaining work. |
| T-03-05-03 | Tampering | fragment assembly | high | mitigate | Require consistent packet ID/count/declared length and complete CRC-valid assembly before delivery. |
| T-03-05-04 | Elevation of Privilege | priority scheduler | high | mitigate | Consume only the explicit validated TrafficClass seam and fixed queue precedence. |
| T-03-05-05 | Spoofing | recovery heartbeat | high | mitigate | Require current epoch/session/sequence and recommit before readiness returns. |
| T-03-05-SC | Tampering | dependency surface | low | accept | No package or service is added. |
</threat_model>

<verification>
- Run the full session Vitest file under Node 22.
- Run `npm run typecheck` under Node 22.
- Confirm all nine named edge truths have explicit deterministic cases.
</verification>

<success_criteria>
- Complete packets cross in both directions exactly once.
- Turns do not intentionally overlap.
- Priority, memory, retry, and recovery behavior is finite and observable.
</success_criteria>

<output>
Create `.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-05-SUMMARY.md` when done.
</output>
