---
phase: 03-negotiate-and-deliver-reliable-acoustic-packets
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/fips/src/transport/mod.rs
  - vendor/fips/src/transport/sound/mod.rs
  - vendor/fips/src/node/lifecycle/mod.rs
  - vendor/fips/src/node/handlers/handshake.rs
  - vendor/fips/src/node/handlers/timeout.rs
  - vendor/fips/src/node/dataplane/peer_actions.rs
autonomous: true
requirements: [LINK-08]

must_haves:
  truths:
    - "A source-authored TrafficClass travels from each narrow FIPS handshake, rekey, heartbeat, and ordinary-data caller through TransportHandle and SoundTransport into FWAV metadata without examining opaque packet bytes."
    - "The existing send API remains an ordinary-data default, while explicit control and heartbeat callers select their class at the source."
    - "The SoundTransport test fixture observes byte-identical opaque payloads and the exact source-selected class for every supported class."
    - "EDGE[LINK-08/unclassified]: control, acknowledgement, FIPS handshake/rekey, and FIPS heartbeat work have an explicit deterministic priority order; no heuristic byte inspection supplies it."
  artifacts:
    - path: "vendor/fips/src/transport/mod.rs"
      provides: "TrafficClass and classified aggregate transport dispatch"
      exports: ["TrafficClass", "TransportHandle::send_classified"]
    - path: "vendor/fips/src/transport/sound/mod.rs"
      provides: "Class-preserving FWAV FIPS_PACKET emission and focused Rust tests"
    - path: "vendor/fips/src/node/handlers/handshake.rs"
      provides: "Explicit FIPS control-class producers"
    - path: "vendor/fips/src/node/lifecycle/mod.rs"
      provides: "Explicit FIPS heartbeat-class producer"
  key_links:
    - from: "vendor/fips/src/node/handlers/handshake.rs"
      to: "vendor/fips/src/transport/mod.rs"
      via: "send_classified(..., TrafficClass::Control)"
      pattern: "TrafficClass::Control"
    - from: "vendor/fips/src/node/lifecycle/mod.rs"
      to: "vendor/fips/src/transport/mod.rs"
      via: "send_classified(..., TrafficClass::Heartbeat)"
      pattern: "TrafficClass::Heartbeat"
    - from: "vendor/fips/src/transport/mod.rs"
      to: "vendor/fips/src/transport/sound/mod.rs"
      via: "Sound dispatch preserves class as non-payload FWAV metadata"
      pattern: "send_classified"
  prohibitions:
    - statement: "MUST NOT infer FIPS traffic class by parsing, prefix-matching, or otherwise inspecting opaque FIPS packet bytes."
      category: "safety"
      status: "unresolved"
      flagged: true
---

## Phase Goal

**As a** demo operator, **I want to** establish a measured and mutually
committed acoustic session between both laptops, **so that** complete FIPS
packets receive safe bounded deterministic half-duplex delivery below the FIPS
boundary.

<objective>
Trace one real FIPS liveness packet from its semantic source through the normal transport aggregate into Sound's FWAV output with an explicit typed priority class.

Purpose: Resolve LINK-08 at the only trustworthy source before the browser scheduler exists, while preserving the complete opaque packet boundary.
Output: A typed Rust `TrafficClass` dispatch seam, explicit caller annotations, class-preserving SoundTransport framing, and focused tests.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-CONTEXT.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-RESEARCH.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-PATTERNS.md
@.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-VALIDATION.md
</context>

## Artifacts this phase produces

- `TrafficClass::{Control, Heartbeat, Ordinary}` and
  `TransportHandle::send_classified`.
- Explicit class producers in handshake/rekey/heartbeat/data-plane call sites.
- SoundTransport FWAV metadata propagation and class-preservation Rust tests.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Trace one classified liveness packet into Sound FWAV</name>
  <read_first>
    - `03-RESEARCH.md` Pitfall 5, Architectural Responsibility Map, and validation row for LINK-08.
    - `03-PATTERNS.md` Mandatory Wave 0 FIPS traffic-class source trace.
    - `vendor/fips/src/transport/mod.rs` around `TransportHandle::send`.
    - `vendor/fips/src/transport/sound/mod.rs` send path and existing focused tests.
    - `vendor/fips/src/node/handlers/handshake.rs` at the caller locations named by `03-PATTERNS.md`.
  </read_first>
  <files>vendor/fips/src/transport/mod.rs, vendor/fips/src/transport/sound/mod.rs, vendor/fips/src/node/handlers/handshake.rs</files>
  <behavior>
    - A failing Rust test sends one handshake/rekey packet with `TrafficClass::Control`, then observes that class and byte-identical payload in the Sound FWAV fixture.
    - The same payload sent through the legacy/default method is observed as `Ordinary`.
    - Non-Sound transports preserve existing behavior when classified dispatch is used.
  </behavior>
  <action>
Add tests first, then define the narrow source-level `TrafficClass` enum and a
classified aggregate send method whose existing send method delegates with
`Ordinary`. Make SoundTransport encode the enum as validated FWAV metadata
outside the opaque payload and make other transports forward bytes through
their existing send implementation. Annotate one actual handshake/rekey caller
with `Control` to complete the tracer. Keep the payload byte-for-byte unchanged
and do not introduce a protocol parser or alternate send path.
  </action>
  <verify>
    <automated>cd vendor/fips &amp;&amp; cargo test sound_transport_traffic_class --locked</automated>
  </verify>
  <acceptance_criteria>
    - The focused test observes Control and Ordinary classes across the real aggregate/Sound path.
    - The test asserts byte-identical payload preservation.
    - Existing non-Sound transport dispatch remains source-compatible.
  </acceptance_criteria>
  <done>One semantic FIPS control packet reaches Sound FWAV with a trustworthy explicit class and unchanged opaque bytes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Classify every liveness and ordinary producer explicitly</name>
  <read_first>
    - `03-PATTERNS.md` caller list for lifecycle, handshake, timeout, and dataplane.
    - `vendor/fips/src/node/lifecycle/mod.rs`, `node/handlers/handshake.rs`, `node/handlers/timeout.rs`, and `node/dataplane/peer_actions.rs`.
    - The `TrafficClass` contract and focused test created by Task 1.
  </read_first>
  <files>vendor/fips/src/node/lifecycle/mod.rs, vendor/fips/src/node/handlers/handshake.rs, vendor/fips/src/node/handlers/timeout.rs, vendor/fips/src/node/dataplane/peer_actions.rs, vendor/fips/src/transport/mod.rs, vendor/fips/src/transport/sound/mod.rs</files>
  <behavior>
    - Handshake and rekey callers produce `Control`.
    - Heartbeat/keepalive callers produce `Heartbeat`.
    - Dataplane callers produce `Ordinary`, including the default compatibility path.
    - Each class survives through Sound's FWAV metadata while the same opaque bytes emerge.
  </behavior>
  <action>
Complete the source trace by annotating every narrow handshake, rekey, and
heartbeat caller identified in research, while keeping the ordinary data plane
explicit or on the documented ordinary default. Add table-driven Rust tests
that exercise every class and reject unknown wire discriminants. Keep the enum
small and semantic; do not leak acoustic queue names into FIPS.
  </action>
  <verify>
    <automated>cd vendor/fips &amp;&amp; cargo test sound_transport_traffic_class --locked &amp;&amp; cargo test transport --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every caller named by the Wave 0 source trace has an intentional class.
    - Unknown wire class metadata rejects before queue mutation.
    - The ordinary default is documented by behavior tests and cannot accidentally gain priority.
  </acceptance_criteria>
  <done>The FIPS side has a complete tested producer-to-Sound traffic-class trace for LINK-08.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| FIPS semantic caller → aggregate transport | Trusted packet purpose becomes scheduling metadata. |
| SoundTransport → local bridge | Untrusted framing must not alter class or opaque bytes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-01-01 | Tampering | FWAV class metadata | high | mitigate | Validate exact enum values before emission/acceptance and test byte-identical payload preservation. |
| T-03-01-02 | Elevation of Privilege | ordinary data caller | high | mitigate | Default all unannotated dispatch to Ordinary and source-annotate only the narrow liveness callers. |
| T-03-01-03 | Information Disclosure | traffic classifier | high | mitigate | Carry source metadata only; forbid packet parsing and expose no payload-derived fields. |
| T-03-01-SC | Tampering | dependency surface | low | accept | This plan adds no package and modifies only the provenance-locked vendored fork. |
</threat_model>

<verification>
- `cd vendor/fips && cargo fmt --check`
- `cd vendor/fips && cargo test sound_transport_traffic_class --locked`
- Confirm every LINK-08 source-trace caller is classified without reading payload bytes.
</verification>

<success_criteria>
- FIPS control and heartbeat semantics reach Sound as typed metadata.
- Ordinary traffic remains the safe default.
- Opaque packet bytes are never inspected or changed.
</success_criteria>

<output>
Create `.planning/phases/03-negotiate-and-deliver-reliable-acoustic-packets/03-01-SUMMARY.md` when done.
</output>
