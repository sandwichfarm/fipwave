---
phase: 02-configure-and-establish-the-codec-neutral-fips-bridge
plan: "07"
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - vendor/fips/src/config/transport.rs
  - vendor/fips/src/config/mod.rs
  - vendor/fips/src/transport/sound/mod.rs
  - vendor/fips/src/transport/mod.rs
  - vendor/fips/src/node/mod.rs
  - vendor/fips/src/control/queries.rs
  - vendor/fips/src/control/snapshots/show_transports.json
autonomous: true
requirements: [FIPS-01, FIPS-02, FIPS-03, CODEC-01]

must_haves:
  truths:
    - "FIPS constructs sound from normal transport configuration and delegates every capability enumerated in COVERAGE.md through TransportHandle."
    - "Sound start, stop, send and receive use the normal FIPS lifecycle and inject complete inbound bytes only through ReceivedPacket on PacketTx."
    - "Configured and link MTU accept exactly 1357 bytes, reject larger packets, and make the node's effective IPv6 MTU exactly 1280 before peer establishment."
    - "Sound configuration and source contracts contain no codec, PCM, browser, fragmentation, ARQ, calibration or waveform fields."
    - "A worker that is Up but lacks an armed browser fails send closed and remains visibly distinct from peer-connected or acoustic-ready."
    - "All 13 capabilities in COVERAGE.md are integrated and verified; none is silently inherited or omitted."
  artifacts:
    - path: "vendor/fips/src/transport/sound/mod.rs"
      provides: "Codec-neutral WebSocket sound transport and Rust behavior tests"
      contains: "ReceivedPacket::new"
    - path: "vendor/fips/src/transport/mod.rs"
      provides: "Exhaustive Sound TransportHandle dispatch"
      contains: "TransportHandle::Sound"
    - path: "vendor/fips/src/node/mod.rs"
      provides: "Sound construction and honest pre-operational MTU selection"
    - path: "vendor/fips/src/control/snapshots/show_transports.json"
      provides: "Control-output contract including sound state and safe statistics"
  key_links:
    - from: "vendor/fips/src/config/transport.rs"
      to: "vendor/fips/src/node/mod.rs"
      via: "TransportsConfig.sound consumed by Node::create_transports"
      pattern: "sound"
    - from: "vendor/fips/src/transport/sound/mod.rs"
      to: "vendor/fips/src/transport/mod.rs"
      via: "SoundTransport stored and exhaustively delegated by TransportHandle::Sound"
      pattern: "TransportHandle::Sound"
    - from: "vendor/fips/src/transport/sound/mod.rs"
      to: "packages/bridge/src/protocol.ts"
      via: "Exact binary FIPS_PACKET envelope proven by the 02-01 tracer"
      pattern: "FIPS_PACKET"
    - from: "vendor/fips/src/node/mod.rs"
      to: "vendor/fips/src/control/queries.rs"
      via: "Constructed transport state, MTU and statistics appear in normal control queries"
      pattern: "show_transports|transport_stats"
---

## Phase Goal

**As an** operator, **I want to** resolve either laptop from one authority and connect FIPS to its local browser through a codec-neutral binary bridge, **so that** complete opaque packets can enter the normal FIPS transport lifecycle without inventing an acoustic-peer claim.

<objective>
Implement sound on the provenance-locked fork as a complete first-class FIPS transport, split into a packet-core slice and an exhaustive aggregate-dispatch slice.

Purpose: Preserve normal FIPS identity, encryption, routing, heartbeat, lifecycle, MTU and control behavior while adding only the complete-packet local bridge adapter.
Output: Sound transport core, exhaustive dispatch/construction/MTU/control integration and executable Rust coverage for all 13 declared capabilities.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-CONTEXT.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-RESEARCH.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-PATTERNS.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/COVERAGE.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-01-SUMMARY.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-02-SUMMARY.md
@packages/bridge/src/protocol.ts
</context>

## Artifacts this phase produces

- A strict codec-neutral `SoundConfig` and owned complete-packet WebSocket transport.
- Normal `TransportHandle::Sound` construction and all 13 delegated capability seams.
- Honest 1357-byte link / 1280-byte effective IPv6 MTU behavior before peer establishment.
- Safe control output that keeps worker lifecycle separate from browser readiness.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Run one complete opaque packet through SoundTransport</name>
  <read_first>
    - `02-RESEARCH.md` Pattern 1, resolved worker-Up/browser-readiness question, Package Legitimacy Audit and Pitfalls 1–2.
    - `02-PATTERNS.md` `SoundConfig` and `sound/mod.rs` analog assignments.
    - `COVERAGE.md` configuration, start, stop, send and receive-injection rows.
    - `packages/bridge/src/protocol.ts`: use the packet envelope established by 02-01.
    - `02-02-SUMMARY.md`: assert the exact vendored base and dependency provenance before modifying it.
    - Vendored `src/config/transport.rs`, `src/transport/mod.rs`, and UDP/TCP/loopback modules.
  </read_first>
  <files>vendor/fips/src/config/transport.rs, vendor/fips/src/config/mod.rs, vendor/fips/src/transport/sound/mod.rs</files>
  <behavior>
    - Test 1: `SoundConfig` rejects unknown fields, non-loopback bridge URLs, MTU below 1357 and arbitrary peer addresses.
    - Test 2: start connects to a local fixture, send preserves an opaque packet, inbound binary packet injects one byte-identical `ReceivedPacket`, and stop cancels/joins owned work.
    - Test 3: an exact 1357-byte send succeeds; 1358 bytes and a disconnected or unarmed browser condition fail closed.
    - Test 4: current-epoch RESET clears owned transport queues/reconnect generation and old-epoch inbound work cannot inject or update counters.
    - Test 5: bounded lifecycle/MTU/congestion/statistics control state is available to the bridge and contains no codec, PCM, browser, fragment or private packet bytes.
    - Test 6: worker lifecycle may be Up while browser readiness is false, but no packet is accepted or counted until a current-epoch browser leg is armed.
  </behavior>
  <action>
Write the missing Rust tests before implementation. Add `SoundConfig` to the existing transport config module with strict serde validation and only bridge URL, configured peer address, MTU and queue bounds. Implement `SoundTransport` as an owned, bounded loopback WebSocket worker that consumes/produces the Phase 02-01 FIPS_PACKET binary envelope, validates before injection, rejects oversize/disconnected/unarmed sends, and delivers inbound data only through `ReceivedPacket::new(...)` on the existing `PacketTx`. Treat worker lifecycle Up and browser readiness as separate fields: Up is allowed for the local worker, but sends fail with the safe disconnected result until a current-epoch browser leg is armed. Handle shared binary RESET by advancing the owned generation, clearing packet/reconnect work and rejecting old-epoch completion. Emit only bounded validated control state for lifecycle, browser readiness, MTU, congestion, counters and safe last error. Preserve complete opaque bytes; do not introduce FIPS sidecar/TUN access, custom RFC 6455 framing, codec details or acoustic reliability behavior.
  </action>
  <verify>
    <automated>cd vendor/fips &amp;&amp; cargo test sound_transport --locked</automated>
  </verify>
  <acceptance_criteria>
    - Start/send/receive/stop pass through the owned sound worker and existing `PacketTx`.
    - Exact 1357-byte acceptance, oversize rejection and disconnected/unarmed failure are under Rust test.
    - RESET invalidates transport queues/generation and old-epoch work.
    - Worker Up remains distinct from browser readiness and cannot authorize a send.
    - Source/config tests prove the Rust boundary contains no codec/PCM/fragment/browser implementation contract.
  </acceptance_criteria>
  <done>The pinned fork contains a tested complete-packet SoundTransport core with strict codec-neutral configuration and fail-closed browser readiness.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete every FIPS transport dispatch, construction, MTU and control seam</name>
  <reversibility rating="costly">The first-class enum and control contract affect every normal transport consumer, but remain internally versioned with the vendored fork.</reversibility>
  <read_first>
    - `COVERAGE.md`: treat every one of its 13 rows as a required implementation checklist; no unmentioned match-arm omission is acceptable.
    - `vendor/fips/src/transport/mod.rs`: inspect the full `TransportHandle` enum and every delegated method.
    - `vendor/fips/src/node/mod.rs`: inspect `create_transports`, operational selection and TUN MTU derivation.
    - `vendor/fips/src/control/queries.rs` and `src/control/snapshots/show_transports.json`: preserve normal control query shape.
    - `vendor/fips/src/transport/sound/mod.rs`: use its tested public interface from Task 1; do not reopen packet framing.
  </read_first>
  <files>vendor/fips/src/transport/mod.rs, vendor/fips/src/node/mod.rs, vendor/fips/src/control/queries.rs, vendor/fips/src/control/snapshots/show_transports.json, vendor/fips/src/transport/sound/mod.rs</files>
  <behavior>
    - Test 1: configured sound constructs exactly one `TransportHandle::Sound`; invalid sound config prevents node startup.
    - Test 2: all 13 COVERAGE.md capabilities dispatch without panic/non-exhaustive omission and use explicit static discovery/connection policy.
    - Test 3: configured MTU 1357 is returned by transport and link queries, permits 1357-byte send, and yields effective IPv6 MTU 1280 before a peer exists.
    - Test 4: worker-up/browser-disconnected, ready, overflow/congested and stopped states remain distinguishable in control output.
    - Test 5: normal control output includes safe sound ID/name/type/state/MTU/link/congestion/stats and contains no packet bytes or secret-bearing URL.
  </behavior>
  <action>
Start with failing construction, dispatch, MTU and control snapshot cases. Add the sound transport type/handle and update every match arm named in COVERAGE.md: lifecycle, send, identity/name/type/state, MTU/link MTU, static discovery, connection policy, congestion and transport statistics. Construct it only through `Node::create_transports`; include configured sound MTU in the pre-operational MTU selection so FIPS honestly exposes 1280 after its 77-byte overhead. Implement discovery as an explicit empty/static result, address policy as the one configured peer, and connection operations with connectionless semantics while keeping local worker Up separate from browser readiness. Extend normal `show transports` control data/snapshot with safe sound state, browser readiness, MTU and counters. Do not mark the peer connected, bypass handshake/heartbeat, or infer acoustic readiness from worker lifecycle.
  </action>
  <verify>
    <automated>cd vendor/fips &amp;&amp; cargo test sound_ --locked</automated>
  </verify>
  <acceptance_criteria>
    - Every one of the 13 COVERAGE.md capabilities is exercised by an exhaustive Sound dispatch test.
    - Node configuration constructs Sound through the same aggregate path as upstream transports.
    - Transport/link MTU 1357 and effective IPv6 MTU 1280 are proven before peer establishment.
    - `show transports` exposes only safe lifecycle/readiness/congestion/statistics facts.
    - No test or control output equates local worker Up with an armed browser, acoustic link or authenticated FIPS peer.
  </acceptance_criteria>
  <done>The vendored FIPS fork treats sound as a complete normal transport across configuration, runtime, MTU, policy, statistics and inspection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local bridge → Rust sound worker | Untrusted binary data may otherwise enter FIPS's trusted packet channel. |
| Sound transport → FIPS node lifecycle | A bypass or false state could evade normal identity/routing/MTU behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-05 | Tampering | Sound receive worker | high | mitigate | Validate packet envelope/type/length/epoch/readiness before exactly-once `ReceivedPacket` injection. |
| T-02-06 | Elevation of Privilege | transport/node integration | high | mitigate | Construct and dispatch only through normal `TransportHandle` and `PacketTx`; tests reject sidecar/TUN bypass. |
| T-02-07 | Denial of Service | reconnect and send queues | high | mitigate | Bound queue items/bytes/age, cancellation and reconnect; oversize/disconnected/unarmed sends fail closed. |
| T-02-08 | Spoofing | configured peer policy | high | mitigate | Accept only the configured sound address; no ambient discovery or arbitrary connect target. |
| T-02-09 | Information Disclosure | control/stat output | medium | mitigate | Allowlist scalar fields and safe counters; omit packet bytes and secret-bearing bridge URL details. |
| T-02-SC | Tampering | vendored FIPS dependency surface | high | mitigate | Consume only the exact 02-02 provenance-locked fork and audited locked dependency graph; add no package. |
</threat_model>

<verification>
- `cd vendor/fips && cargo fmt --check`
- `cd vendor/fips && cargo test sound_ --locked`
- `cd vendor/fips && cargo test --locked`
- Confirm COVERAGE.md reports 13/13 INTEGRATE rows and each proof command names an executable sound test.
</verification>

<success_criteria>
- Complete opaque packets use the normal transport lifecycle in both directions.
- The full 13-capability upstream transport surface is covered without omissions.
- MTU and control surfaces remain honest before browser arming or peer establishment.
- Worker Up never authorizes traffic while browser readiness is false.
</success_criteria>

<output>
Create `.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-07-SUMMARY.md` when done.
</output>
