---
phase: 02-configure-and-establish-the-codec-neutral-fips-bridge
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - vendor/fips/
  - vendor/fips/UPSTREAM.md
  - vendor/fips/Cargo.toml
  - vendor/fips/Cargo.lock
autonomous: true
requirements: [FIPS-01]

must_haves:
  truths:
    - "The repository contains an immutable, license-preserving FIPS source snapshot at commit fc8ebd5a06d6f042c57f03107f403116365a16b4."
    - "The imported tree preserves the upstream license, pinned Rust toolchain and a mechanically checkable pre-patch identity."
    - "The only new direct Rust dependency is the audited exact tokio-tungstenite 0.30.0 release, resolved in the committed lockfile without a second async runtime."
    - "Every local patch is listed in provenance so later transport work has an explicit, reproducible base."
  artifacts:
    - path: "vendor/fips/UPSTREAM.md"
      provides: "Immutable upstream URL, commit, source date, license, toolchain and local patch inventory"
  key_links:
    - from: "vendor/fips/UPSTREAM.md"
      to: "vendor/fips/"
      via: "Immutable URL/commit/import recipe and complete local patch inventory"
      pattern: "fc8ebd5a06d6f042c57f03107f403116365a16b4"
    - from: "vendor/fips/Cargo.toml"
      to: "vendor/fips/Cargo.lock"
      via: "Exact audited direct dependency and locked transitive resolution"
      pattern: "tokio-tungstenite"
---

## Phase Goal

**As an** operator, **I want to** resolve either laptop from one authority and connect FIPS to its local browser through a codec-neutral binary bridge, **so that** complete opaque packets can enter the normal FIPS transport lifecycle without inventing an acoustic-peer claim.

<objective>
Mechanically import and provenance-lock the exact FIPS integration target before any transport behavior is patched.

Purpose: Give every later Rust task one reproducible, licensed and audited source/dependency base that can be verified independently of sound behavior.
Output: The pinned fork, immutable provenance record and locked Rust dependency graph.
</objective>

<execution_context>
@/Users/sandwich/.codex/gsd-core/workflows/execute-plan.md
@/Users/sandwich/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-CONTEXT.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-RESEARCH.md
@.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-PATTERNS.md

<interfaces>
The pinned upstream defines `PacketTx = tokio::sync::mpsc::Sender&lt;ReceivedPacket&gt;`, `ReceivedPacket::new(transport_id, remote_addr, data)`, the `Transport` trait, and the concrete `TransportHandle` enum dispatcher.
`TransportHandle` owns start, stop, send, ID/name/type/state, MTU/link MTU, discovery, auto-connect, accept-connections, connect/connection-state/close, congestion and transport-stat dispatch. `Node::create_transports` constructs configured handles and its startup MTU path chooses the effective TUN MTU.
The Phase 2 Package Legitimacy Audit approves exact `tokio-tungstenite = "=0.30.0"` from crates.io; no `[ASSUMED]`, `[SUS]` or `[SLOP]` package is present.
</interfaces>
</context>

## Artifacts this phase produces

- A normal repository-local FIPS source snapshot with immutable provenance.
- A mechanically checkable pre-patch identity and authorized local patch inventory.
- Locked Rust dependency and reproducible test/build inputs.

<tasks>

<task type="auto">
  <name>Task 1: Import and provenance-lock the exact FIPS fork</name>
  <reversibility rating="costly">Later phase patches build directly on this exact vendored base; changing it requires rebasing and revalidating the local patch inventory.</reversibility>
  <read_first>
    - `02-RESEARCH.md` Summary, Standard Stack, Package Legitimacy Audit and Alternatives Considered.
    - `02-PATTERNS.md` `UPSTREAM.md`/`Cargo.toml` assignment and pinned upstream dependency conventions.
    - Pinned upstream root `LICENSE`, `Cargo.toml`, `Cargo.lock` and `rust-toolchain.toml` at commit `fc8ebd5a06d6f042c57f03107f403116365a16b4`.
    - Phase 1 npm audit/lock pattern: retain immutable source, version, integrity/license and offline-verifiable provenance.
  </read_first>
  <files>vendor/fips/ (immutable upstream snapshot), vendor/fips/UPSTREAM.md, vendor/fips/Cargo.toml, vendor/fips/Cargo.lock</files>
  <action>
Import the normal source snapshot for exact commit `fc8ebd5a06d6f042c57f03107f403116365a16b4` without upstream history, preserve its MIT license and pinned Rust 1.94.1 toolchain, and write `UPSTREAM.md` with URL, immutable commit, source date, license, import procedure and every locally patched file per C-05. Verify the unpatched snapshot identity before applying local changes. Add only the Package Legitimacy Audit-approved exact `tokio-tungstenite = "=0.30.0"` direct dependency, reuse upstream Tokio/futures, update the committed lockfile and document that no `[ASSUMED]`, `[SUS]` or `[SLOP]` package is installed. Do not clone/fetch mutable master during normal builds.
  </action>
  <verify>
    <automated>cd vendor/fips &amp;&amp; cargo metadata --locked --format-version 1 &gt;/dev/null &amp;&amp; cargo test --no-run --locked</automated>
  </verify>
  <acceptance_criteria>
    - The imported upstream files match the exact commit before the documented local patch list.
    - MIT license, exact source commit/date and Rust toolchain are present and offline readable.
    - Locked metadata resolves `tokio-tungstenite` exactly at 0.30.0 and adds no second async runtime.
    - The pinned unmodified test targets compile before sound behavior is added.
  </acceptance_criteria>
  <done>The repository contains a buildable, provenance-locked FIPS fork and audited Rust dependency graph.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Vendored upstream/crate registry → build | Imported source and new Rust dependency become executable code. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-SC | Tampering | vendored FIPS and cargo install | high | mitigate | Exact upstream commit/provenance plus Package Legitimacy Audit OK for `tokio-tungstenite=0.30.0`, locked resolution and license verification. |
</threat_model>

<verification>
- `cd vendor/fips && cargo metadata --locked --format-version 1`
- `cd vendor/fips && cargo test --locked`
- Inspect `cargo metadata --locked` for the exact audited dependency and unchanged Rust toolchain.
</verification>

<success_criteria>
- The pinned fork is reproducible and licensed.
- Upstream identity is mechanically verified before local patches.
- The dependency graph is locked, audited and introduces no second async runtime.
- Provenance names every local patch consumed by later plans.
</success_criteria>

<output>
Create `.planning/phases/02-configure-and-establish-the-codec-neutral-fips-bridge/02-02-SUMMARY.md` when done.
</output>
