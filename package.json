{
  "name": "fips-over-sound",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": "22.23.1"
  },
  "scripts": {
    "verify:dependencies": "node scripts/audit-dependencies.mjs --check-lock package-lock.json",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc --noEmit",
    "test:unit": "vitest run",
    "test:browser": "playwright test --pass-with-no-tests",
    "test:skeleton": "vitest run tests/skeleton.test.ts",
    "build": "vite build",
    "generate:corpus": "node --input-type=module --eval \"console.log('Corpus generation is introduced by Plan 01-04.')\" --",
    "qualify:fixture": "node --input-type=module --eval \"console.log('Fixture qualification is introduced by Plan 01-05.')\" --",
    "qualify:verify": "node --input-type=module --eval \"console.log('Physical qualification verification is introduced by Plan 01-07.')\" --",
    "test:compose": "node --input-type=module --eval \"console.log('Compose TUN preflight is introduced by Plan 01-06.')\" --",
    "check": "npm run verify:dependencies && npm run lint && npm run typecheck && npm run test:unit && npm run test:browser && npm run build && npm run generate:corpus -- --check && npm run qualify:fixture && npm run qualify:verify && npm run test:compose"
  },
  "dependencies": {
    "ws": "8.21.1"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "@types/node": "26.1.1",
    "@types/ws": "8.18.1",
    "eslint": "10.7.0",
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
